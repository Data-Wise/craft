# Fix-Scope Extension Report

**Date:** 2026-06-23
**Branch:** feature/skill-standards
**Task:** Add safe fixes #2 (frontmatter normalization) and #3 (TOC stub) to `apply_safe_fixes`

## What Was Done

### Fix #3 — TOC stub insertion (finding-driven)

For each `size`-category finding whose message indicates a reference file > 300 lines with no TOC:

- Inserts `## Table of Contents\n\n<!-- Sections in this reference; keep in sync. -->` immediately after the first H1 (`# ...`) if present, else at the top.
- Uses the auditor's TOC regex to confirm insertion satisfies the check.
- Re-audit after fix produces no size finding for the file.

### Fix #2 — Frontmatter key normalization (blanket pass)

For every `root.rglob("SKILL.md")`:

- Safety check: if any line in the frontmatter matches block scalar (`:\s*[|>]`) or continuation/list patterns, the file is skipped entirely (byte-identical).
- For simple frontmatter: lowercases keys that case-insensitively match `VALID_SKILL_KEYS`, preserving the value text byte-for-byte using `partition(":")`.
- Reorders into canonical order: `name`, `description`, `when_to_use`, then remaining keys in original relative order.
- Writes only if the normalized block differs.

## Tests Added (4 new, TDD)

| Test | Behavior Verified |
|------|-------------------|
| `test_fix_inserts_toc_stub_in_oversized_reference` | 350-line ref without TOC gets stub; re-audit clean |
| `test_fix_normalizes_frontmatter_order_and_casing` | `Description:` lowercased to `description:`, reordered after `name:` |
| `test_fix_preserves_description_value` | Value string byte-identical after fix |
| `test_fix_skips_block_scalar_frontmatter` | File with block-scalar description left byte-identical |

## Files Changed

- `scripts/skill_standards_audit.py` — added `_normalize_frontmatter()`, `TOC_STUB`, `TOC_RE`, `_SIMPLE_KV`, `_BLOCK_SCALAR`, `_CONTINUATION`; expanded `apply_safe_fixes()` with fixes #2 and #3
- `tests/test_skill_standards.py` — added `import re`; added 4 new tests (lines 105–152)
- `commands/code/skill-standards.md` — updated `--fix` description and Auto-Fix Mode section to document all three fixes

## Test Results

20 passed, 0 failed. Command-audit: no errors for skill-standards.
