{# Shared conftest.py partial — included by project-type conftest templates #}
"""Pytest configuration and shared fixtures for {{ project_name }}.

Auto-generated by /craft:test:gen — customize freely after generation.
"""

import os
import sys
from pathlib import Path

import pytest

# Add project root to sys.path
_project_root = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _project_root)

# Add tests/ directory for helper imports
_tests_dir = os.path.dirname(os.path.abspath(__file__))
if _tests_dir not in sys.path:
    sys.path.insert(0, _tests_dir)


@pytest.fixture
def project_root() -> Path:
    """Path to the project root directory."""
    return Path(_project_root)


@pytest.fixture
def temp_dir(tmp_path: Path) -> Path:
    """Provide a clean temporary directory (pytest-managed, auto-cleaned)."""
    return tmp_path
