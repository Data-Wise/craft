status: Active
version: 2.56.0
release_date: 2026-07-01
milestone: v2.56.0 SHIPPED 2026-07-01 — PR #234 (dev→main merge `43beb7b3`, tag v2.56.0): token-usage reduction (PR #232 squash `e6c24a27` — orchestrator model pinning, `/refine` 631→42 lines, `/brainstorm` redesign, new `command-skill-token-efficiency` skill) + release-tracking hardening (namespace-refactor token-cost go/no-go measured 68.3% reduction; broken `/usage` scheduled-trigger replaced with `ccusage`+`claude-monitor`; 2 cross-spec factual bugs fixed via a second grill pass). MINOR bump (feat: #232). All CI checks GREEN on first pass — no `--admin` needed (Validate Plugin Structure resolved cleanly this time). Full suite 2089 passed; pre-release-check PASSED; main CI GREEN; live site confirmed v2.56.0; homebrew-release workflow SUCCESS; post-release sweep ALL CLEAN. Counts 117/43/8. Prior: v2.54.0 SHIPPED 2026-06-27 — `/craft:workflow:brief` (117th command): 3-line action block generator (Next step / Watch out for / Connects to) with `--plan`/`--verbose`/`--show-context` flags; `--brief` integrated into `/craft:do` Step 5.5. 29 e2e + 12 dogfood tests. Main CI GREEN; docs deployed; homebrew tap updated (117 commands); post-release sweep CLEAN (getting-started.md fixed); all BLOCK surfaces aligned. Counts: 117 / 39 / 8. Prior: v2.53.0 SHIPPED 2026-06-26 — multi-surface-aware release (surface registry + aggregator CI propagation + Cowork verify leg + `/craft:dist:surfaces`, #218/#184) + docs-staleness fix (docs/plans/ excluded from count-consistency scan). MINOR bump (feat). All tests pass; main CI GREEN; docs deployed; homebrew tap updated (116 commands); post-release sweep ALL CLEAN; aggregator sync wired (all 4 satellite repos). Counts 116/39/8. Prior: v2.52.0 SHIPPED 2026-06-26 — interactive-by-default commands (#215) + default-on test/docs scaffolding (#216) (PR #217 dev→main merge `c5f704f3`, tag v2.52.0, `--admin` on stuck Validate-Plugin-Structure): grill/orchestrate AskUserQuestion model (Recommended-first + per-option consequence) + `--yes`/`--non-interactive`; refine default-on for brainstorm/do/plan:feature/grill (opt-in elsewhere) + `--yes` cascade + orchestrate Step 0.5 structured model; AND brainstorm/plan:feature/grill emit a tier-inferred test-plan + doc-scorer Documentation section BY DEFAULT (`--no-tests`/`--no-docs`; arch:plan/spec-review opt-in via `--tests`/`--docs`), logic in skills (brainstorm-insights/plan-orchestrator, deprecation-trap-safe + guarded), doc-scorer REUSED, lifecycle-split, count-cascade-excluded. Built spec→grill→plan→subagent-driven-TDD (2 plans, 7+8 tasks, per-task spec/quality + opus whole-branch reviews; gates caught a dropped-phrase regression, a refine over-flip, a decorative deprecation-guard sentinel, and a MEMORY.md over-compaction that dropped 37/102 pointers → all fixed). MINOR bump (4 feat). Full suite 1990 passed; pre-release-check PASSED; main CI GREEN; docs deployed (live site 2.52.0); Homebrew Release auto-updated tap; verify-surfaces 6/6 ALIGNED (refreshed local lag: brew formula 2.51→2.52 via `--formula`, marketplace-update-THEN-plugin-update for the Code pin); post-release sweep ALL CLEAN. Counts 115/39/8. Prior: v2.51.0 SHIPPED 2026-06-25 — governance retro (#205) + skill-standards release gate (PRs #211/#212→dev, #214 dev→main merge `180181f8`, tag v2.51.0; CLOSED #205 + #138 via default-branch auto-close): D1 **R09 status-not-drift** governance check (`governance/checks/status_drift.py`, warn) — audits `.STATUS` `version:` vs manifest + SHIPPED/tagged vX.Y.Z vs git tag, fixture `--selftest` + LIVE in `pre-release-check.sh` (this very release was self-gated by it: ".STATUS: version + release claims match" ✓); D3 ADR-003 (release-drift advisory not hard-gate + P7 freeze); skill-standards validator graduated to a **release-tier gate** (`CRAFT_MODE=release` blocks, advisory below) + fixed a latent CRAFT_MODE no-op (LLM-prose command never exported the gating var → gate+version-check silently dead). MINOR bump (2 feat: #211/#212). Full suite 1970 passed; pre-release-check PASSED; main CI GREEN; **verify-surfaces 6/6 ALIGNED** (after refreshing local lag: brew upgrade 2.50→2.51, `claude plugin update` 2.50→2.51, pulled stale `~/projects/dev-tools/homebrew-tap` checkout — the script reads that local clone FIRST, was the only "blocker"); post-release sweep ALL CLEAN. Counts 115/39/8. Process: merged #211/#212 (conflict-free despite branches behind dev — `--stat` looked conflicted but they authored ZERO changes to `.STATUS`/`CLAUDE.md`/`governance.md`; verified via `merge-tree --write-tree`), pushed a local-only gate branch first (data-loss risk), `git:clean` removed 2 worktrees (11+2 squash-merged branch refs left dangling — branch-guard blocks `-D`, harmless). Prior: v2.50.0 SHIPPED 2026-06-25 — v2.49.x issues sprint (PRs #206/#207/#208→dev, #209 dev→main --admin on phantom Validate Plugin Structure, tag v2.50.0): advisory skill-standards validator in `/craft:check` (hot-reload, never fails); homebrew dist-gates `verify_caveats.py`+`post_install_check.py` wired into `/release` Steps 10b/10c (advisory, strict via HOMEBREW_GATE_STRICT) + 10d (BLOCKING aggregator-sync + Cowork verify) (#200/#199); SessionEnd insights facet hook + idempotent installer, per-session-id dedup (#183); ADR-001 Workflow-vs-worktree (#171); +14 e2e/dogfood tests pinning locked decisions. Built via craft:grill (10 decisions D1–D10) → superpowers writing-plans (4 TDD tracks) → 2 dynamic Workflows (impl+verify, ran from dev session under branch-guard bypass, re-protected after). MINOR bump (3 feat: commits). Full suite 1958 passed; pre-release-check PASSED; main CI GREEN; tap+brew+Code+marketplace all 2.50.0 (verify-surfaces 6/6 ALIGNED); post-release sweep CLEAN. KEY INCIDENTS (recovered): (1) #206 squash folded UNPUSHED local-dev base commits → #207/#208 phantom conflicts → rebased --onto origin/dev to untangle (memory squash-folds-unpushed-dev-base-commits; push dev before PRing off it); (2) GitHub comma-list `Closes #200, #199, #183, #171` only auto-closed #200 (+#171 via Track-0 trailer) — each issue needs its OWN keyword; closed #183/#199 manually. Counts 115/39/8. Prior: v2.49.0 SHIPPED 2026-06-23 — skill-standards auditor + /done memory/settings + ADR-002 + release re-split (PR #203 auditor→dev, #204 release dev→main --admin, tag v2.49.0): `/craft:code:skill-standards` batch-audits `skills/**/SKILL.md` vs a vendored copy of Anthropic's standards (score+exit 0/1/2, `--json`/`--markdown`, `--fix` = version-tag-strip + frontmatter-normalize + TOC-stub, never prose; `--refresh-standards`); `/done` Memory Optimize (Step 1.12) + Settings Sync (Step 1.10.5); ADR-002 consolidated `/done` into the `adhd-workflow` skill (`references/done.md` SoT + thin shim); 4 oversized skills split to `references/` (release re-split loss-gated after a lossy first attempt, 0 lines lost) → 39 skills 100/100. Counts 114→115. Release: dev was unsynced with main (v2.48.0 release merge commits) → dev-wins sync merge resolved the PR #204 conflict; full suite 1929 passed; main CI GREEN; docs deployed; tap synced. POST-RELEASE TOOLING (homebrew-tap + savant): root-caused the local Code-registered lag — the formula `post_install` ran `claude plugin update` WITHOUT first `claude plugin marketplace update local-plugins` (no-op on a stale cache); fixed in `homebrew-tap/generator/generate.py` for ALL claude-plugin formulas; reconciled manifest-drift across 6 formulas (rforge-orchestrator's `deprecate!` was at risk of silent un-deprecation; himalaya-mcp 12 skills/22 MCP tools; scholar 28→34; craft `{command_count}` single-source token) gated by regen==committed; added a Formula Drift Guard CI workflow (regen-vs-committed, first run GREEN); savant `CLAUDE.md` 19→25 skills. Memories: post-install-marketplace-refresh-before-update, deprecated-command-rich-body-trap, skill-authoring-tooling-landscape. Prior: v2.48.0 SHIPPED 2026-06-23 (grill + orchestrate Step 0.5 Clarify). Prior: v2.47.0 SHIPPED 2026-06-21 — Governance roadmap COMPLETE (PR #196/#197→dev, #198→main, tag v2.47.0): R04 automated as content-drift (manual→checks/no_drifted_copy.py — installed SKILL.md must be byte-identical to canon; DISTINCT from R07 version-pin, catches a hand-edited body even when the version matches; gates session, vacuous-skip otherwise) + #184 release pre-flight advisory (pre-release-check.sh runs run_rules.py --json, prints RED count, NEVER blocks — gentle-ramp; TestReleaseGuard184 invariant) + live-flow test coverage (e2e soak lifecycle hook→ledger→promote-check + R04-in-audit; dogfood ledger edges). Governance suite 47→59. KEY FINDING (memory r04-content-drift-not-filetype): naïve "real-dir=copy" heuristic was dead (installs are real dirs, no symlinks) → content-drift; always fire a new WARN checker on the live env first (a false positive poisons the soak ledger). #198 merged CLEAN (no --admin). Homebrew tap synced to v2.47.0, main CI GREEN, post-release sweep ALL CLEAN. Counts: 113 / 39 / 8. Governance now: R01–R04+R06 automated · R05 advisory · R07 external · R08; 5 surfaces (visibility·prevention·promotion·cross-repo·release-advisory). Prior: v2.46.0 SHIPPED 2026-06-21 — Governance Phase 2 PR #3 (PR #194→dev, #195→main, tag v2.46.0): soak-then-flip promotion + cross-repo wrapper, completing Phase 2 (visibility · prevention · promotion · cross-repo). soak.py = LOCAL gitignored STATE.json ledger (first_seen/last_seen/last_red per rule) fed by the SessionStart hook each audit; run_rules.py --promote-check (--window 14d, --state) lists warn rules soaked clean long enough to recommend a human warn→error flip (machinery recommends, human promotes; advisory exit 0). run.sh = cross-repo wrapper (consumers invoke the one installed engine, cwd-portable, no drift, R07). 8 dogfood tests (injected today → no flake), 47 governance suite green. Bundled #193: test_performance_cached_run → xfail(strict=False) (wall-clock microbenchmark flaked on shared CI, forced --admin; now a soft signal) — and #195 MERGED CLEAN with NO --admin (the xfail paid off). Homebrew tap synced to v2.46.0, main CI GREEN, post-release sweep ALL CLEAN. Counts: 113 / 39 / 8. R04 automation + #184 release-pre-flight guard remain deferred. Prior: v2.45.0 SHIPPED 2026-06-21 — Governance Phase 2 PR #2 (PR #190→dev, #192→main, tag v2.45.0): SessionStart visibility hook (governance/session_hook.py) — audits the live ~/.claude/skills tree at session open and injects a compact RED-only summary into context via SessionStart additionalContext; silent when clean, mtime-cached, no-op where skills tree absent, never breaks a session on error. Visibility not prevention (SessionStart hooks inject context, can't block). Completes the three-surface model: pre-commit + CI (prevention) · SessionStart (visibility). Implemented direct-to-worktree from the dev session (worktree-path-aware). #192 admin-merged (strict up-to-date BEHIND from the v2.44.0 merge-commit divergence; PR-context full suite GREEN). Homebrew tap synced to v2.45.0, main CI GREEN, post-release sweep ALL CLEAN. Counts: 113 / 39 / 8. Global install of the hook DEFERRED to post-v2.45.0 (point at released plugin path; docs in governance/README.md). PR #3 (soak-then-flip + cross-repo wrapper) + R04 deferred. Also: removed leaked ORCHESTRATE-guard-suite.md (PR #191). Prior: v2.44.0 SHIPPED 2026-06-21 — Governance Phase 2 PR #1 (PR #188→dev, #189→main, tag v2.44.0): R03 automated (manual→script — checks/no_private_in_public_marketplace.py scans a marketplace manifest, fails on a public-marketplace ref to a private/PII repo, denylist {savant}, dual-mode file/dir + good/bad fixtures; closes the last error-rule enforcement gap), pre-commit governance-gate hook (^governance/ → selftest + render --check drift) + CI gate in ci.yml (with explicit "CI does NOT evaluate R01/R07 live-env" note), engine {marketplace} substitution through audit()+selftest() + --marketplace. Built from a draft→adversarial-verify→ultra-finalize workflow chain (verified packet); implemented directly into the worktree from the dev session (branch-guard is worktree-path-aware — corrected a stale assumption). Also corrected stale "not yet wired" governance docs. #189 admin-merged (only failure was the flaky test_performance_cached_run timing test, unrelated to the diff; PR-context full suite GREEN). Homebrew tap synced to v2.44.0, main CI GREEN, post-release sweep ALL CLEAN. Counts: 113 commands / 39 skills / 8 agents. Phase 2 PR #2 (SessionStart hook + soak-then-flip + cross-repo wrapper) NEXT; R04 deferred. Prior: v2.43.0 SHIPPED 2026-06-20 — Skill-Ecosystem Governance (Phase 0) (PR #185→dev, #187→main, tag v2.43.0): policy-as-code under governance/ — RULES.yaml (8 skill-location rules, single source of truth), run_rules.py (audit + --selftest meta-validation, fail-closed), render_rules.py (CLAUDE.md block generation + --check drift gate), portable checkers + good/bad fixtures, guide + Plugin Release Runbook, 24 e2e/dogfood tests + governance pytest marker. PR #185 review hardening (all 5 issues): fail-closed gating (broken checker on error rule exits 1), recursive broken-symlink detection, R01 gates session (not ci) + visible canon-skip, --check exit-2, --selftest surfaces external rules, UTF-8. Homebrew tap synced to v2.43.0, main CI GREEN, post-release sweep ALL CLEAN. Counts: 113 commands / 39 skills / 8 agents. Phase 2 enforced-gates roadmap spec open in PR #186. Prior: v2.42.0 SHIPPED 2026-06-19 — /craft:code:fewer-prompts curated read-only Bash allowlist (PR #181). Prior: v2.41.1 SHIPPED 2026-06-19 — Documentation Coverage (PR #180): 25 tutorial stubs for all Phase 8 commands + broken link fix in TUTORIAL-smart-help.md. Prior: v2.41.0 SHIPPED 2026-06-19 — token-efficiency (quota gate, engine flag, parity runbook). v2.40.0 SHIPPED 2026-06-19 — Guard Suite (PR #174): /craft:git:guard + guards.json registry & ADHD toggle UX, no-switch-guard promoted to scripts/, guard-consistency validator, install-guards.sh. Destructive-restore reconciled to no-switch-guard; branch-guard rm-rf-.git/reset--hard softened to confirm (catastrophic ops stay NON-muteable). 111 commands. Review found 1 CRITICAL (catastrophic-bypass) + 6 issues — ALL fixed pre-merge; +5 registry tests; ci.yml now installs both guards + runs the no-switch suite. Prior: v2.39.0 SHIPPED 2026-06-19 — doc-coverage enforcement (PR #172→dev, #173→main). Prior: v2.38.2 SHIPPED 2026-06-15 — multi-surface release arc (3 patches). v2.38.0: multi-surface engine (verify-surfaces Step 13.6, cache-prune Step 13.7, Data-Wise aggregator marketplace) + branch-guard draft=dev (PRs #158/#159/#160/#161). Adversarial review found 3 HIGH + 6 MEDIUM/LOW bugs → v2.38.1 (PR #163: branch-aware dogfood tests fixed RED main CI; cache-prune data-loss; verify-surfaces git-tag no-op) → v2.38.2 (PR #165: corrupt-surface blocks, symlink GC, arg-parse, subshell). All 3 → main, main CI GREEN, tap + aggregator (Data-Wise/claude-plugins) pinned to 2.38.2, post-release sweep CLEAN. Counts: 112 commands / 39 skills / 8 agents. Prior: v2.37.0 SHIPPED 2026-06-13 — drift-prevention chain + CI triage/watch. Bundled brainstorm items 1-7 (PRs #151/#152/#153/#154/#155): #1 strict-markers guard + #4 workflow recipe discoverability; #7 bump-version categorical-subtotal sweep; #2 CI count-drift tripwire (README live counts); #5 /craft:ci:triage + #6 /craft:ci:watch. PR #156 → main 8c21a54d (merge commit), release tag v2.37.0. Downstream all GREEN: main CI verified, homebrew-release.yml updated tap to v2.37.0 (desc "112 commands", url v2.37.0.tar.gz). Post-release sweep ALL CLEAN (drift tooling paid off). Counts: 112 commands / 39 skills / 8 agents. Prior: v2.36.0 SHIPPED 2026-06-13 (PR #150 → main eb7e143d).
last_session: 2026-07-01
last_completed: v2.56.0 release (`43beb7b3`) + doc-drift sweep (2 real bugs: stale index.md placeholder, fictional brainstorm CLI syntax in tutorial+refcard) + all docs-staleness warnings resolved to GREEN + committed SPEC-superpowers-claude-context-workflows-2026-07-01.md (`199900b1`, next task: grill before implementing). Prior: install-branch-guard.sh deployment gap fix (d01dd1e8) + squash-merge tests (3f5e62f1) — flat-copy installs now deploy lib/git-utils.sh to ~/.claude/lib/; is_squash_merged() resolves in production (was silently undefined, causing every git branch -D to fall through to confirm prompt); BSD grep -P incompatibility fixed in tests. CHANGELOG [Unreleased] updated: hooks skill (40th), headless recipes, install gap fix, git SKILL.md Op 4. Prior: release skill improvements (e7ecda27) — Step 3b /craft:docs:update --post-merge; Step 3b.5 docs-staleness-check.sh --non-interactive gate; Step 9 mkdocs build --strict; Step 13 curl live-site version check. Prior: #228 quality gates (a6e08a01) — squash-merge branch cleanup (lib/git-utils.sh + branch-guard.sh), hooks skill (MkDocs --strict PostToolUse template), headless recipes doc (3 claude -p recipes + GHA snippet). Skills 39→40.

progress: v2.34.0 SHIPPED 2026-05-15 (PR #142 → main as 25c53c95, release tag v2.34.0). Bundled: commands→skills migration via PR #139 (3 batches, 11 new skills auto-activating from conversation context), PR #140 (post-merge drift sweep + 9th over-counter fix), PR #141 (skill coverage docs in skills-agents.md + bump-version sweep for `(N total)` headers + 10th + 11th over-counter instances), site refresh + .STATUS sync (4c7a44bd, 2b25c2a1), CHANGELOG promotion + tagline cleanup (cbb1a740). Test suite: 19 unit tests on test_craft_plugin.py (added test_skill_count_predicates_use_canonical_marker as a regression net for the recurring SKILL.md-vs-`*.md` over-counting bug — now fixed in 11 utilities total). Downstream all GREEN: docs.yml deployed, homebrew-release.yml updated tap to v2.34.0, brew install verified locally. Post-release sweep: ALL CLEAN, no Tier 2 drift.

📊 Branch Status:

| Branch | Head     | Base | Status                                                              |
|--------|----------|------|---------------------------------------------------------------------|
| main   | 43beb7b3 | —    | Production (**v2.56.0 tagged 2026-07-01**, PR #234 merged, main CI GREEN, docs site + Homebrew tap confirmed live) |
| dev    | 43beb7b3 | main | Synced with main post-release (fast-forwarded, no drift). 2026-07-01 |

📌 Next action (held, not yet started): `/craft:orchestrate:plan` against `docs/specs/GRILL-plan-orchestrator-dispatch-mode-2026-07-01.md` (synthesize SPEC from the ledger's 10 branches — no paired SPEC file exists) to plan the `orchestrate-dispatch` mode. **User to review the grill ledger/synthesized spec first before this runs.** Refined prompt captured in session 2026-07-01 (prompt-refiner applied manually — skill not invocable via Skill tool this session, known discovery gap).

✅ Resolved 2026-07-01: **Command-namespace naming rule.** Decided *against* a plugin-wide rename (craft already ~90% `namespace:command`, no depth-3 sprawl; renaming saves ~0 resting tokens per the bloat grill; count-cascade + reference-rot + discoverability cost). Instead adopted the **verb-vs-flag convention** (distinct operation → subcommand; variant → flag) as advisory guidance in `CONTRIBUTING.md`. Rationale: [BRAINSTORM-command-namespace-naming-rule](docs/specs/BRAINSTORM-command-namespace-naming-rule-2026-07-01.md). Optional follow-up (unscheduled): promote the convention to an advisory governance rule (`governance/RULES.yaml` + checker) if enforcement is wanted.

💡 Backlog (cross-plugin, not scheduled): **Teaching-residue audit (craft ↔ scholar).** The 2026-07-01 consolidation audit ([BRAINSTORM-command-skill-consolidation-audit](docs/specs/BRAINSTORM-command-skill-consolidation-audit-2026-07-01.md)) found teaching residue in craft after teaching moved to the `scholar` plugin (teaching-only as of v3.0.0). Two parts: (1) **teaching-specific utils** — `utils/{semester_progress,teach_config,teaching_validation}.py` + `utils/readme-{teach-config,semester-progress}.md` — check whether `scholar` now owns these, then migrate/delete the stale ones; (2) **de-teaching-ify framing** on general commands that kept teaching wording — `git/status.md` ("teaching-specific context") and the `site/*` namespace ("Publish teaching site") — keep the general Quarto/pkgdown/MkDocs capability, drop the teaching-only wording. Higher-value than shim retirement (which the same audit found not worth it — all 56 shims are referenced). **Cross-plugin: must inspect scholar's surface before craft deletes/migrates anything.** Captured 2026-07-01.

📂 Active Worktrees:

| Branch | Path | Purpose |
|--------|------|---------|
| _(prior, MERGED)_ | — | `feature/grill-skill` → dev via [#238](https://github.com/Data-Wise/craft/pull/238) (squash), 2026-07-01. Extracted grill into `skills/workflow/grill/SKILL.md` (thin-command/fat-skill, ADR-002) + thin-shimmed `commands/grill.md` + folded in the attack-angles feature. Skill count 43→44. All 5 CI checks passed clean (no `--admin`). Worktree removed, local branch deleted. |
| `feature/plugin-audit-skill` | `~/.git-worktrees/craft/feature-plugin-audit-skill` | Created 2026-07-01 (base `dev` @ 79a91897). Linear-plan Step 4 / task #4: new `skills/code/plugin-audit/SKILL.md` catching installed-plugin duplicates (the class of bug that missed `workflow@local-plugins`). PR [#237](https://github.com/Data-Wise/craft/pull/237) open, **HELD** — `/review` found the skill doesn't work against the real `installed_plugins.json` schema and its collision heuristic produces a false-positive firehose (verified live); needs rework before merge, not a quick fix. |
| _(prior, MERGED)_ | — | `feature/deprecated-cmd-audit-batch1` → dev via [#236](https://github.com/Data-Wise/craft/pull/236) (squash `16863789`), 2026-07-01. `commands/check.md` + 6 commands consolidated into `skills/dev/git/references/`. Rebased onto origin/dev pre-merge (`.STATUS` conflict resolved), `/review` findings fixed (TOC headers), CI (Validate Plugin Structure) passed. Worktree removed, local branch deleted. |
| _(prior, MERGED)_ | — | `feature/token-probe-script` → dev via [#235](https://github.com/Data-Wise/craft/pull/235) (squash `222647c7`), 2026-07-01. `scripts/token-probe.py`. Worktree removed, local branch deleted. |
| _(prior, none active)_ | — | `feature/token-usage-reduction` MERGED to dev via [#232](https://github.com/Data-Wise/craft/pull/232) (squash e6c24a27); worktree (`craft-review`) removed, local + remote branch deleted. Orchestrator model routing, `/refine`+`/brainstorm` redesign, `command-skill-token-efficiency` skill, namespace-refactor token-cost addendum. Follow-ups: `.STATUS` next-action D (post-merge `/usage` checkpoint) + E (Validate Plugin Structure root-cause). |

_Recent (MERGED to dev 2026-06-25, squash `f41c11d5`, `--admin` on stuck non-substantive Validate-Plugin-Structure — all substantive CI green, worktree removed + local branch deleted): `feature/spec-scaffold-defaults` (PR [#216](https://github.com/Data-Wise/craft/pull/216), Plan 2 of 2) — `brainstorm`/`plan:feature`/`grill` emit a tier-inferred test-plan + doc-scorer Documentation section by DEFAULT (`--no-tests`/`--no-docs` opt out); `arch:plan`/`spec-review` opt-in (`--tests`/`--docs`). Logic in skills (brainstorm-insights/plan-orchestrator) + shared `references/scaffold-templates.md`; deprecated bodies stay thin (ADR-002, guarded); doc-scorer reused (commands/docs/sync.md); lifecycle-split + count-cascade-excluded. 8 tasks subagent-driven (per-task + opus whole-branch review); full suite 1958 green. Final review hardened the deprecation-guard sentinel (`change shape`) + corrected the scorer doc-type list. Side-incident fixed: Task 7 over-compacted MEMORY.md (dropped 37/102 pointers) → repaired. **Both plans (#215/#216) now on dev — release pending (`/release`).**_

_Recent (MERGED to dev 2026-06-25, squash `3c3446d6`, all CI green, worktree removed + local branch deleted): `feature/interactive-commands` (PR [#215](https://github.com/Data-Wise/craft/pull/215), Plan 1 of 2) — grill/orchestrate AskUserQuestion model + `--yes` non-interactive flag; refine default-on for brainstorm/do/plan:feature/grill (opt-in for orchestrate/orchestrate:workflow/arch:plan), refine scope 6→7; `--yes` cascade; orchestrate Step 0.5 structured model. 13 new e2e/dogfood tests + bumped test_refine_flag_scope; full suite 1948 green. Built subagent-driven (7 tasks, per-task + opus whole-branch review). Pushed dev BEFORE merge to avoid the unpushed-base squash-fold footgun. **NEXT: Plan 2 (scaffold-defaults) — `docs/plans/2026-06-25-spec-scaffold-defaults.md`, depends on this `--yes`/default-on plumbing.**_

_Recent (both MERGED to dev 2026-06-25, conflict-free squash, no `--admin`; remote branches deleted; worktrees removed 2026-06-25; local branch refs left dangling — squash-merge + branch-guard blocks `-D`, harmless): `feature/governance-retro-205` (PR #211 → dev `51327ffc`, #205 retro R09 status-drift + ADR-003 D1–D4) + `feature/check-skill-standards-gate` (PR #212 → dev `d483484f`, skill-standards release-tier gate + CRAFT_MODE no-op fix). Verified mergeable via `merge-tree --write-tree` despite branches being behind dev — the `--stat` looked like `.STATUS`/`CLAUDE.md`/`governance.md` conflicts but the branches authored ZERO changes there (behind ≠ changed); dev's R09 doc `57af82a8` not clobbered. Both branches had been pushed first (gate branch was local-only — would have been a data-loss risk)._

_Recent (all 3 merged to dev + worktrees removed 2026-06-24): v2.49.x issues sprint — `feature/skill-standards-check` (PR #206, advisory validator in /craft:check), `feature/homebrew-dist-gates` (PR #207, verify-caveats + post_install gates + blocking aggregator-sync, #200/#199), `feature/insights-session-hook` (PR #208, SessionEnd facet hook + installer, #183). Built via craft:grill (10 decisions) → superpowers writing-plans (4 tracks) → 2 dynamic Workflows (impl+verify). ADR-001 + #171 shipped in Track 0. NOTE: #206 squash folded in unpushed local-dev commits; #207/#208 rebased onto origin/dev to untangle phantom conflicts. Remote branches deleted._

_Recent (merged + worktree removed 2026-06-22): `feature/grill-command` (PR #201, `/craft:grill` command + orchestrate Step 0.5 + lean split + count cascade 113→114 → dev)._

_Recent (merged + worktree removed 2026-06-21): `feature/governance-test-coverage` (PR #197, live-flow e2e + soak edge cases → dev)._

_Prior (all merged + worktrees removed 2026-06-21): `feature/governance-184-release-guard` (PR #196, R04 content-drift automation + #184 release advisory → dev); `feature/governance-phase2-soak` (PR #194, soak-then-flip + cross-repo `run.sh` → dev); `feature/flaky-perf-xfail` (PR #193, xfail timing test); `feature/governance-phase2-hook` (PR #190, SessionStart hook → dev), `feature/cleanup-stray-orchestrate` (PR #191, removed leaked ORCHESTRATE), `feature/governance-phase2-gates` (PR #188, v2.44.0). Local branch refs left dangling (squash-merge + branch-guard blocks `-D`) — harmless._

> **🎯 Next Action (next session):**
> A) **Spec-review + brainstorm `SPEC-superpowers-claude-context-workflows-2026-07-01.md`** (Recommended) — committed `199900b1` 2026-07-01 (also at `docs/internal/PROPOSAL-superpowers-claude-context-workflows.md`, byte-identical duplicate, not yet reconciled). Proposes: (1) correct an external report's overbroad "don't adopt Superpowers" verdict — `subagent-driven-development` is load-bearing (every `docs/plans/*.md` requires it as a sub-skill), only the orchestrate/brainstorm/clarify-design-plan-code-verify layer is genuinely redundant with `/craft:orchestrate` + `/craft:grill`; (2) pilot `claude-context` MCP (zero namespace clashes with craft/savant) on `~/projects/r-packages/` as a semantic-search complement to Grep/Glob; (3) a reusable token-probe script. **Status: "Ready to implement" per the doc itself, but per this repo's process (spec → grill → plan), needs a `/craft:grill` pass before any implementation starts** — do not skip straight to Task 1-4 in the doc.
> B) **`brew upgrade craft && claude plugin update craft@local-plugins`** — v2.56.0 shipped 2026-07-01; refresh local install lag (advisory, WARN surfaces, not blocking).
> C) **Brainstorm craft skills for plugin release + Desktop chat** — raised 2026-06-26; explore new skills/commands for plugin release workflow automation and Desktop chat integration surface.
> D) **`--swarm=2` orchestrate level** — `SPEC-orchestrate-swarm-2level-2026-06-18` APPROVED; implement 2-level autonomous subagent dispatch.
> E) **Check real `/usage` data ~2 weeks after PR #232 merges** (target: ~2026-07-14) — `npx ccusage daily --since 2026-07-01` (post-merge) vs. `npx ccusage daily --since 2026-06-25 --until 2026-06-30` (pre-merge baseline, already exists) against the unvalidated 48% line-count hypothesis. Record the result in `SPEC-token-efficiency-research-2026-06-30.md` §9, not a new doc. (Scheduled-trigger approach abandoned — `anthropics/claude-code` #43438/#40460/#53581, platform-wide, not session-specific.)
> F) **Investigate/fix the "Validate Plugin Structure" CI check** — passed cleanly on both PR #232 and the v2.56.0 release PR #234 this time (no `--admin` needed either time), but has a documented history of hanging. Watch the next few PRs before considering this resolved; root-cause if it recurs.

✅ Session 2026-06-30 (token-usage-reduction branch — pushed, PR #232 open, awaiting human review/merge):
- **Token-usage research + 4-commit fix** (`9abdd1b1`, `0e076e66`, `c7f9010e`, `039530b5`): model-pinned orchestrator agents + extracted resilience templates to a skill; `/refine` 631->42 lines (thin shim, ADR-002 pattern); fixed fictional `subagent_type` names in `/brainstorm`'s old max-depth path; full `/brainstorm` redesign (split `brainstorm` from `brainstorm-insights`, 4->2 decision points, removed in-skill agent delegation in favor of the existing `--orch` flag).
- **Verification pass + 5th commit** (`81750e05`): ran the FULL pytest suite (~2056 tests, not just the targeted files) against the branch for the first time — found and fixed 2 real test regressions the redesign introduced (`test_skill_trigger_phrases_unique`, `test_refine_flag_documented`) plus 1 from the earlier orchestrator-v2 extraction (`test_behavior_9_timeline`). Also fixed skill-count drift (dev was already 44 vs documented 40 BEFORE this branch; branch's 2 new skills pushed it to 42) across plugin.json/marketplace.json/CLAUDE.md/README.md/7 docs files/mkdocs.yml/package.json/REFCARD.md (REFCARD checked separately by `bump-version.sh --verify`, missed on first pass), and stale `/brainstorm` syntax in commands/hub.md + docs/commands/hub.md + docs/skills-agents.md (still showed the removed max-depth shorthand). `validate-counts.sh`, `bump-version.sh --verify`, `docs-staleness-check.sh` all clean after.
- **SPEC-craft-audit-and-next-steps-2026-06-30.md** (6th commit, this entry): full repo scan of all 56 deprecated commands' body-size ratio vs their `replaced-by:` skill (script committed: `scripts/audit-deprecated-commands.py --threshold 2.0`). Top finding: `skills/dev/git/` (250 lines) is the `replaced-by:` target for 6 commands totaling 4185 lines (`worktree.md` 1010, `init.md` 597, `sync.md` 539, `docs/safety-rails.md` 723, `docs/learning-guide.md` 722, `docs/undo-guide.md` 594) — highest-leverage place to start a pre-v3.0.0 consolidation pass. `commands/check.md` (1132 lines -> 127-line skill, ratio 8.9) is the single worst ratio in the repo, not previously flagged. Proposes a new WARN-only governance rule generalizing this check (ADR-002's fix made durable instead of one-off).
- **RESOLVED (was BLOCKED)**: GitHub MCP connector still has no working `GITHUB_TOKEN`/`GH_TOKEN`, but Desktop Commander (separate tool, runs directly on the Mac) has its own working `gh` CLI auth. Pushed via `git push -u origin feature/token-usage-reduction`, opened **PR #232** (`base: dev`), filed **issue #233** tracking the audit findings. Real worktree created at `../craft-review` (no plumbing tricks needed — confirms the sandbox `unlink()` restriction was sandbox-specific, not a real repo issue: same stale lock file + dangling worktree that blocked the sandbox were removed instantly from the Mac).
- All 6 commits built via `git commit-tree` + manual ref writes — this session's sandbox couldn't `unlink()` files under `.git/` (lock files, temp objects). Worth confirming normal `git worktree add`/`git commit` works cleanly on the real Mac (should — sandbox-specific, not a repo issue). See SPEC §4.3.
- **7th commit**: relocated the 3 loose root-level session artifacts into proper repo paths -- `docs/specs/SPEC-craft-audit-and-next-steps-2026-06-30-REVIEW.md` (review companion to the spec), `docs/plans/2026-06-30-token-usage-reduction-handoff.md` (session-continuation prompt), `docs/internal/GROUNDING-craft.md` (durable project-context prompt, added to docs/internal/README.md's table + mkdocs.yml nav -- caught by docs-staleness-check.sh's nav_completeness phase before commit). Also drafted `docs/plans/2026-06-30-ISSUE-draft-audit-handoff.md` -- a ready-to-file GitHub issue body, since issue creation also requires the same blocked GitHub auth (filed for real later this session, see below).
- **8th commit (this entry)**: pushed branch + opened **[PR #232](https://github.com/Data-Wise/craft/pull/232)** (`feature/token-usage-reduction` -> `dev`) and filed **[issue #233](https://github.com/Data-Wise/craft/issues/233)** from the already-drafted content (labels adjusted to the repo's real set: `documentation`,`enhancement` -- `tech-debt`/`governance` don't exist as labels here). Updated `docs/specs/SPEC-craft-audit-and-next-steps-2026-06-30-REVIEW.md` with a dated UPDATE block closing out the stale "cannot push/PR" claim rather than rewriting the original record. All done via Desktop Commander on the real Mac -- no sandbox plumbing needed for this round. Root-level scratch-file duplicates on the real Mac's `dev` worktree (GROUNDING-craft.md, HANDOFF-token-usage-reduction.md, REVIEW-token-usage-reduction.md, skills/orchestrator-resilience/, skills/workflow/brainstorm/, token-reduction-plan.md) removed -- `dev`'s working tree now matches its committed HEAD exactly. **Remaining**: human diff review of PR #232 before merge (CI as of last check: Markdown Linting/Link Validation/Docs Staleness SUCCESS, Validate Plugin Structure still IN_PROGRESS); unrelated second worktree at `~/.git-worktrees/craft/flamboyant-visvesvaraya-d043ee` (detached HEAD `ea17a758`, dated 2026-06-29, clean tree, pre-dates this session) intentionally left untouched, not this session's to remove. Also noted: local `dev` has 1 unpushed commit (`62f1d0cf`, CHANGELOG sync) ahead of `origin/dev` -- unrelated to this branch, flagging so a future PR-2-into-dev doesn't get confused by it.

✅ Session 2026-06-30 (PR #232 review→merge→cleanup + `/usage` checkpoint replacement):
- **PR #232 adversarial review + fix**: 8 findings (`/review` + `/code-review`) across `commands/hub.md`, `docs/commands/hub.md`, `docs/REFCARD.md`, `docs/skills-agents.md`, `docs/adr/ADR-002-...md`, `tests/test_scaffold_defaults_dogfood.py`, both CHANGELOGs, 2 skill files — all fixed and verified via full suite (2057 passed, 0 failed) before merge. **PR #232 squash-merged to dev as `e6c24a27`** (2026-07-01); Craft CI + Validate Plugin Structure both passed cleanly this time (no `--admin` needed); worktree (`craft-review`) + local/remote branch removed after verifying squash-merge completeness via tree-diff.
- **Namespace-refactor token-cost probe**: empirical go/no-go measurement (243→77 tokens, 68.3% reduction measured; ~531 tokens projected at scale) appended as an addendum to `docs/specs/SPEC-refactor-namespace-2026-06-29.md` (commit `86e98a61`) rather than a new doc.
- **`/usage` checkpoint tooling replacement**: the scheduled-trigger mechanism (`send_later`/`create_trigger`) returned persistent 404s — confirmed via research as a known, currently-open platform-wide bug (`anthropics/claude-code` #43438/#40460/#53581), not session-specific. Replaced with `ccusage` (npx, no-install, verified working) + `claude-monitor` (PyPI package — NOT `claude-code-usage-monitor`, that's only the GitHub repo name; installed via `uv tool install claude-monitor`, both smoke-tested live 2026-06-30). Grilled and spec'd first (`docs/specs/GRILL-usage-checkpoint-tooling-2026-06-30.md`, 5 branches); per explicit instruction, merged the standalone spec into `SPEC-token-efficiency-research-2026-06-30.md` §9 instead of leaving a second doc, then deleted the standalone file.
- **Second grill pass on the finished SPEC** (`docs/specs/GRILL-token-efficiency-research-spec-audit-2026-06-30.md`) caught 2 real cross-spec bugs: a factual disagreement (18 vs. "~20 of 56" flagged commands — live script re-run settled it at 18, fixed in `SPEC-craft-audit-and-next-steps-2026-06-30.md:74`) and a stale §8 bullet not reflecting the §9 fix. Logged, not fixed: `spec_crosslink()` can't discover a grill target that got merged into a *different* pre-existing spec — accepted as a known limitation, no automation currently depends on it.
- **Net result**: no PR needed for any of this (all `.md`, allowed direct-commit on `dev`) — but `dev` is now 17 commits ahead of `main`/v2.55.0, unreleased. See Next Action A.

✅ Session 2026-06-29 (plugin audit + superpowers research + CLAUDE.md health fix):
- **Plugin audit** (60 installed plugins → 33 KEEP / 24 DELETE): rendered ADHD visual widget with copy-paste `claude plugin remove` commands. All 12 `@claude-code-workflows` flagged DELETE (redundant with craft's 117 commands); 3 language LSPs (Java/Lua/PHP) not in stack; 3 duplicate entries across sources.
- **Superpowers research**: confirmed official Anthropic source (`anthropics/claude-plugins-official`). Plugin = behavioral framework (14 skills, 0 commands); `using-superpowers` meta-skill injected at session start via system prompt — installing = activating, no command needed. Decision: KEEP — complements craft (superpowers = HOW/discipline, craft = WHAT/tooling).
- **CLAUDE.md health fix** (`0a35ffec`, pushed to dev): `claude_md_sync.py` required `["Quick Commands", "Project Structure", "Testing"]` sections; added Project Structure + Testing tables (87→108 lines). Run with `PYTHONPATH=. python3 utils/claude_md_sync.py --dry-run`.
- **Obsidian tutorial** (created via obsidian-ops MCP): "Superpowers + Craft — Workflow Tutorial and Cookbook" saved to `Knowledge_Base/dev-tools/claude-code/`. 9 skill-to-command pairings, 4 end-to-end cookbook recipes (add command, fix failing test, release v2.55.0, complex unknowns), anti-patterns table.

_Prior: `feature/multi-surface-release` MERGED → dev `3814525d` (PR #161 squash, 2026-06-15, CI green) — all 5 phases (verify-surfaces engine, cache-prune, aggregator marketplace craft-side, docs; rforge drift fix ops). Full suite 1769 passed. ORCHESTRATE removed at merge (`a75dea82`); worktree removed; local branch `feature/multi-surface-release` left dangling (squash + unpushed rebase → `-d` refuses, `-D` guard-blocked — harmless). **Cross-repo aggregator DONE: `Data-Wise/claude-plugins#3` MERGED → main `45ab799d`** (2026-06-15) — `marketplace.json` listing craft/scholar/rforge/himalaya-mcp now live; `claude plugin marketplace add Data-Wise/claude-plugins` surfaces all. Q6 resolved to reuse claude-plugins. `feature/branch-guard-cleanup` MERGED → dev `772efcc7` (PR #160 squash, 2026-06-15, CI green) — #158/#159 review follow-up: consolidated dev/draft detection (single probe, `refs/heads/*`), test-header note + e2e re-indent; worktree + branches removed. `feature/ci-triage` MERGED → dev `a1812afb` (PR #154 squash, 2026-06-14, CI green) — item #5; first real test of #7+#2 (auto-swept CI subtotal, tripwire caught stale refs). `feature/pr2-drift-tripwire` MERGED → dev `f00f5872` (PR #153 squash, 2026-06-14, CI green) — completes the #7→#2 chain (subtotals synced + CI tripwire). `feature/pr7-bump-subtotals` MERGED → dev `91a82271` (PR #152 squash, 2026-06-13; CI caught a real BSD-sed-no-ops-on-GNU bug, fixed with a `sedi()` wrapper). `feature/pr1a-test-hygiene` MERGED → dev `813855f2` (PR #151 squash, 2026-06-13, CI green); worktree + local + remote branch all removed. `fix/mermaid-markers` MERGED → dev `d21a1e4a` (PR #149 squash); `feature/workflow-engine` MERGED → dev `caadd8db` (PR #148 squash) — worktrees + branches removed. `feature/orchestrate-drive`, `feature/refine-flag`, `fix/version-sync-pipefail` merged 2026-06-03 (PRs #143–#145)._

✅ Session 2026-06-27 (post-v2.54.0: #210 hook dedup fix + #184 dist-extras private-plugin support):
- **#210 SessionEnd double-count fix** (`d288b380`): `hooks/session-facet.sh` dedup only matched its own filename scheme (`session-$SESSION_ID.json`), never `/done`'s timestamp-named files (`session-YYYYMMDD-HHMMSS.json`). Added D5 content scan (`grep -rl "\"session_id\": \"$SESSION_ID\""`) after the two filename checks; stamps the marker on match so future invocations hit the fast path. Added regression test `test_no_double_write_when_done_wrote_first` to `tests/test_session_stop_hook.py`.
- **#184 dist-extras: private-plugin mode + pin refresh + drift doctor** (`e7a7e38d`): added three sections to `skills/distribution/dist-extras/SKILL.md`: (1) `### Public vs private plugin mode` — auto-detect via `gh repo view --json visibility`, scaffold a self-marketplace (`source: "./"`) for private repos, hard-guard against publishing a private repo to a public marketplace, private install recipe for Cowork surface; (2) `### Post-release pin refresh` — mandatory `claude plugin marketplace update` + `plugin update` after every release; (3) **Drift doctor** bash snippet for `dist:marketplace validate --drift` — compares `plugin.json` version vs `~/.claude/installed_plugins.json` pin. Closes #184.
- Pre-commit MD032 auto-fix intercepted the dist-extras commit; re-staged and NEW-committed (per convention, no amend). Both commits on `dev`.

✅ Session 2026-06-25 (post-v2.50.0: skill-standards gate + #205 governance retro + insights/hook) — work on 2 UNMERGED feature branches:
- **skill-standards release-tier gate** (`feature/check-skill-standards-gate` `6f19147c`, 77 e2e+dogfood green): graduated the v2.49.0 advisory validator to BLOCK at the release tier (`CRAFT_MODE=release`), advisory below. Fixed a latent **CRAFT_MODE no-op** (check.md is LLM-prose with no env-setting orchestrator → `--for release` never exported the gating var; added an explicit "Mode binding REQUIRED" section that also backstops version-check) + a mislabeled audit exit code (2 = error-findings, not "engine error"). Advisor-caught.
- **#205 governance retro (D1–D4)** (`feature/governance-retro-205` `3a8ebbba`+`cb80c8b7`): grill-free brainstorm→spec (`docs/specs/SPEC-governance-retro-205-2026-06-25.md`) re-scoped 2 of 4 items after a code review (item 2 largely pinned already; item 4 premise false — no double-write). D1 **R09-status-not-drift** check (`governance/checks/status_drift.py`, warn): audits `.STATUS` `version:` vs manifest + SHIPPED/tagged vX.Y.Z vs git tag; FIXTURE (expected.json) for selftest, LIVE fires direct in `pre-release-check.sh` (the shared `{target}`=skills surface vacuous-skips it — avoided a dead rule). D2 named soak-poison promotion test. D3 ADR-003 (release-drift advisory + P7 freeze marker). D4 README hook-reconciliation note. **1934-pass full suite; LIVE drift verified via mutate-and-revert** (advisor caught that fixtures bypassed `manifest_version()`). Caught+fixed a regex false-positive on real `.STATUS` (`v2.37.0.tar.gz`). Docs: governance.md R09 (`57af82a8` on dev).
- **CLAUDE.md sync** (`dc7fd06f`): stale/mis-summed test subtotals (142=36+23+29+55, actual 24/31/42/59) → durable `pytest tests/` pointer. Version+counts were already current (release bump).
- **Insights + #183 SessionEnd hook WIRED**: facet corpus was n=1 (only `/done` writes facets); ran `scripts/install-session-facet.sh` → `SessionEnd` in `~/.claude/settings.json` (verified surgical: only that key added; backup at `~/.claude/settings.json.bak-1782411645`). Filed **#210** (the hook + `/done` use different facet names → double-count a `/done` session).
- **Memory**: 2 new (`verify-gate-trigger-wired-not-just-logic` — a craft gate/rule can be fixture-green yet a production no-op, verify the trigger on REAL inputs; `uv-craft-governance-tests-need-pyyaml`). **Obsidian**: principle note in `Documents/Software_Engineering/`.
- Both feature branches PUSHED, UNMERGED. dev clean+synced. Recurring theme (advisor earned its keep twice): "green" ≠ "fires in production" — verify the trigger, not just the logic.

✅ Session 2026-06-23 (v2.49.0 release + cross-repo tooling):
- **v2.49.0 SHIPPED** (PR #204 → main `2aa6cdbe`, --admin on phantom Validate Plugin Structure; tag v2.49.0). Built via brainstorm→SPEC→superpowers writing-plans→subagent-driven-development (8 TDD tasks, per-task review).
- **`/craft:code:skill-standards`** (PR #203 → dev): stdlib scanner of `skills/**/SKILL.md` vs vendored `docs/reference/SKILL-STANDARDS.md` (frontmatter/size/hygiene; score+exit 0/1/2; `--json`/`--markdown`; `--fix` = version-tag-strip + frontmatter-normalize + TOC-stub, never prose; `--refresh-standards`). Delegates to skill-creator + plugin-dev:skill-reviewer. 23 tests.
- **`/done` enhancements:** Memory Optimize (Step 1.12: orphans/ghosts/stale/dupes + atomic index rebuild) + Settings Sync (Step 1.10.5: read-only drift). **ADR-002:** consolidated into `adhd-workflow` skill (`references/done.md` SoT + thin shim; slash + NL paths converge).
- **Skill compliance pass → 39 skills 100/100:** split 4 oversized skills (`release` 1308→282, `openapi-spec-generation` 1030→195, `ci` 686→268, `changelog-automation` 554→149). **Caught + reverted a lossy release split (86 lines dropped), re-did it loss-gated (line-level detector, 0 lost).**
- **Release pipeline:** dev was unsynced with main (v2.48.0 release-merge commits) → PR #204 conflict → dev-wins sync merge → full suite **1929 passed** → --admin merge → GitHub release → docs deployed (live v2.49.0) → tap synced → post-release sweep CLEAN. Post-merge count sweep (114→115 in mkdocs/package.json/marketplace.json/index.md long-tail).
- **POST-RELEASE TOOLING (homebrew-tap `2540183`, savant `9d66caf`):** root-caused the local Code-registered version lag — formula `post_install` ran `claude plugin update` WITHOUT a prior `claude plugin marketplace update local-plugins` (no-op on stale cache). Fixed in the tap **generator** (all claude-plugin formulas) + reconciled manifest-drift across 6 formulas (gated regen==committed; rforge-orchestrator `deprecate!` rescued, himalaya 12 skills/22 tools, scholar 28→34, craft `{command_count}` token) + added a **Formula Drift Guard** CI (regen-vs-committed, GREEN, proven both directions). savant `CLAUDE.md` 19→25 skills. All 6 surfaces ALIGNED at v2.49.0; cache-prune removed 8 old dirs.


- **v2.48.0 SHIPPED** (PR #202 → main `4dea8ac8`, --admin on phantom Validate Plugin Structure). All substantive CI green: Craft CI ✅, Homebrew Release ✅, Docs Staleness ✅, Link Validation ✅, Markdown Linting ✅, Deploy Documentation ✅, gh-pages ✅.
- **PR #201 merged → dev** (`2727c576`): `/craft:grill` adversarial interrogation command (codebase-first sweep, recommended answer per question, `/done` halt + milestone checkpoints, durable `GRILL-*.md` decision ledger, `/craft:plan` handoff; args: `target`, `--bound N`, `--no-capture`); orchestrate **Step 0.5 Clarify** (invokes `--bound 2 --no-capture` grill before planning, `--no-clarify` to suppress); orchestrate lean split (862-line → ~340-line contract + `docs/reference/orchestrate-reference.md`). Count cascade: 113→114 commands.
- **Release pipeline complete:** post-merge docs sync (0 issues) → bump-version.sh (14 files) → dual-CHANGELOG `[Unreleased]` → `[2.48.0] — 2026-06-23` → pre-release check PASSED → commit `9b803adb` → PR #202 → merge → GitHub release tag → Homebrew tap updated (v2.48.0, SHA256 `76b3af9c`, desc "114 commands") → brew upgrade verified (2.47.0 → 2.48.0) → brew audit --strict clean → post-release sweep ALL CLEAN.
- **New files:** `commands/grill.md`, `commands/utils/grill_ledger.py`, `docs/reference/orchestrate-reference.md`, `docs/commands/grill.md`, `docs/tutorials/TUTORIAL-grill.md`, `tests/test_grill_ledger.py`, `tests/test_grill_command.py`, `tests/test_orchestrate_refactor.py`. Test suite: 1869 passed, 0 failures.
- **Deferred (W4 fast-follow):** ADR + CONTEXT.md glossary on-the-spot capture — not started.

✅ Session 2026-06-19 (v2.42.0 release: fewer-prompts):
- **v2.42.0 SHIPPED** (PR #182 → main, `90f5b45d`). All CI green: Craft CI ✅, Docs Staleness ✅, Link Validation ✅, Markdown Linting ✅, Deploy Documentation ✅.
- **Release pipeline complete:** pre-flight → version bump (14 files) → semantic docs (CHANGELOG, index.md, mkdocs.yml) → PR #182 → merge conflict resolution (dev/main diverged: PR #181 squash-merged to main while dev had different history) → merge → GitHub release tag → docs auto-deployed via docs.yml → Homebrew tap auto-updated via homebrew-release.yml CI → post-release sweep ALL CLEAN.
- **Content shipped:** `/craft:code:fewer-prompts` — curated read-only Bash allowlist command (20 entries) with `--dry-run`, `--global`, `--reset` flags; idempotent via `craft_allowlist` tracking key. Stale-link CHANGELOG fix (test_no_broken_links was failing on markdown syntax in v2.41.1 entry).
- **Resolved:** GitHub issue #157 closed (branch-guard draft=dev — shipped PR #158, issue was stale).
- **Divergence note:** PR #181 (feature→main squash) ran before dev was synced, causing dev/main divergence at release time. Resolved by merging main into dev pre-release, then squash-merge proceeded cleanly.

✅ Session 2026-06-19 (v2.41.1 release: documentation coverage):
- **v2.41.1 SHIPPED** (PR #180 → main, `3e7ae7ab`). Merged with `--admin` (known "Validate Plugin Structure" phantom check). All substantive CI green: Link Validation ✅, Docs Staleness ✅, Markdown Linting ✅, Deploy Documentation ✅.
- **Release pipeline complete:** pre-flight (142 tests + 15 metadata checks) → version bump (14 files) → semantic docs (CHANGELOG, REFCARD, index.md, mkdocs.yml Tier 2 drift) → PR #180 → merge → GitHub release tag → docs auto-deployed via docs.yml → Homebrew tap auto-updated via homebrew-release.yml CI → post-release sweep ALL CLEAN.
- **Content shipped:** 25 tutorial stub files for all Phase 8 commands with mkdocs.yml nav entries; broken link fix in `TUTORIAL-smart-help.md` (TUTORIAL-do.md → smart-routing-tutorial.md, renamed v2.30.0). REFCARD.md Tier 2 drift fixed (counts were stale: 109 cmd/38 skills → 112/39).
- **Session context:** continuation from prior context-compaction session; CI was in_progress on PR #180 at handoff point.

✅ Session 2026-06-18 (orchestrate spec arc: session-lever reconciliation + 3 new specs):
- **Reconciled token-efficiency spec with session-level levers.** Revision note on `SPEC-orchestrate-token-efficiency-2026-06-17`: a global `CLAUDE_CODE_SUBAGENT_MODEL=sonnet` (highest priority) OVERRIDES orchestrator-v2's per-task haiku routing (Explore/Bash/test → haiku), forcing cheap reads up to sonnet (~5× regression) — so do NOT set it globally; craft self-routes. Main-session `Workflow`+`budget` documented as a flow-level practice (not craft — commands can't call it). Default-engine decision rule: derivable spec → `:workflow`, parallel writes → `--swarm`, else fan-out; worktrees = isolation, not a token lever.
- **3 new specs on `dev` (all pushed):** `SPEC-orchestrate-decomposition-2026-06-18` (design RESOLVED — mode-scaled agent soft cap, single-concern + ≤K-file sizing, reuse confirm gate, one-shot planning, fan-out path only); `SPEC-orchestrate-swarm-2level-2026-06-18` (READY — `--swarm` takes optional level, default 1, `--swarm=2` for two levels; bottom-up convergence + per-level test gate + non-overlap guard); `RESEARCH-hierarchical-worktrees-2026-06-18` (nested-dir worktrees = git anti-pattern; real shape = hierarchical branches over flat sibling worktrees, cap 2).
- **Commits:** `711e5a2c` (reconciliation) → `0a432dc1` (decomposition) → `6618c469` (research) → `88de8b4f` (2-level swarm). flow-cli `1907aadb`: dropped `CLAUDE_CODE_SUBAGENT_MODEL` export. Implementation of all three deferred to worktrees (Next Action E).

✅ Session 2026-06-17 (token-efficiency spec/plan + quota tooling + scheduled cloud run):
- **Investigation → APPROVED spec + 16-task plan.** From "is orchestrate the token-usage culprit?" → finding: cost is `agents × inherited-context × turns`; prompt caching is the only structural discount; worktrees cost ZERO tokens (corroborated by Anthropic's published multi-agent numbers). `docs/specs/SPEC-orchestrate-token-efficiency-2026-06-17.md` (APPROVED) + sibling `SPEC-context-floor-hygiene-2026-06-17.md` (skeleton) + `SPEC-workflow-as-default-engine-2026-06-17.md` (SUPERSEDED). Plan `docs/plans/2026-06-17-orchestrate-token-efficiency.md` (16 TDD tasks). All commits pushed to `dev`.
- **Adversarial review earned its keep twice:** killed an infeasible "platform `Workflow()` engine" premise (craft dispatches via Task; no host Workflow tool callable from a command) → re-scoped to craft's real deterministic `:workflow` mode as default (parity-gated); and caught a stale-quota trap before it shipped into Phase Q.
- **Methodology hardened (user pref):** parity gate is estimation-based (effect sizes + 95% CI + surprisal, NO NHST/p-thresholds); cost-weighted metric (output 5×, cache_read 0.1×, cache_creation 1.25×); `.craft/` run-marker reuse (not `.flow/`).
- **Phase Q appended:** `/craft:quota` pre-flight gate (historical median+interval estimate vs LIVE rate_limits) advising SAFE/TIGHT/DEFER; flow-cli `dash` consumer is an out-of-scope producer/consumer follow-on.
- **Quota/limits question resolved:** the native statusline `rate_limits` field (5h/weekly % + resets, Pro/Max after first API response) is THE programmatic source — no public quota CLI/API exists (user's own archived feature-request confirms); claude-hud's Keychain→oauth/usage cache was stale (frozen March). Memory added: `claude-code-native-statusline-rate-limits`, `craft-no-platform-workflow-tool`, `stats-estimation-not-nhst`.
- **Scheduled a cloud routine** (`trig_017orFX3wJv4QborUvFGtvHS`, Mon Jun 22 09:00 MDT, sonnet-4-6) to implement the pure-code tasks (parser + estimator + persister) TDD-style and open a PR to `dev` — deliberately timed for AFTER the weekly quota reset (user on Max 20x; pacing heavy work around the window).

✅ Session 2026-06-15 (branch-guard draft=dev shipped + multi-surface-release spec/worktree):
- **PR #158 MERGED → dev** — `branch-guard.sh` now treats research `draft` as `dev` (smart protection: new code blocked, existing + `.md` + commits allowed, force/reset blocked). Bundled 5 bash regression cases (Group 2b) + 1 pytest auto-detect case (`test_repo_with_draft_protects_both`). Backs the `~/.claude/rules/draft-as-dev-research.md` rule directly — no more manual self-enforcement.
- **PR #159 MERGED → dev** — wired the branch_guard bash suites into `ci.yml`: new "Install branch-guard hook" step + "Run branch-guard bash suites" (`tests/test_branch_guard.sh` + `_e2e.sh`) with `SKIP_PERF=1` gating the 50-invocation timing test. branch_guard behavior is now CI-validated, not local-only.
- **Brainstorm + SPEC (approved): multi-surface release.** Read-only investigation of craft's plugin dev→release pipeline → `docs/specs/SPEC-multi-surface-release-2026-06-15.md` (`9df211ed`). Key finding: Claude **Code** and **Desktop/Cowork** use SEPARATE plugin registries; Desktop CAN subscribe to a custom GitHub marketplace by `owner/repo` (`addMarketplace`, same engine as Code) — so "one source → all surfaces" is achievable via the `marketplace.json` craft already ships + a one-time `claude plugin marketplace add`. Item 1 (Desktop spike) DONE; D1–D7 decisions + 6 work items.
- **Worktree scaffolded:** `feature/multi-surface-release` + `ORCHESTRATE-multi-surface-release.md` (5 phases: rforge drift fix, verify-surfaces engine, cache-prune, aggregator marketplace, docs). Impl pending a NEW worktree session (ORCHESTRATE-STOP rule).
- **Memory:** new `claude-desktop-vs-code-plugin-stores.md` (separate stores + addMarketplace) so the investigation isn't redone. Global rule `draft-as-dev-research.md` updated (hook now enforces draft=dev, PR #158).

✅ Session 2026-06-13 (mermaid-marker CI fix + workflow-engine doc-gap closure):
- **PR #149 MERGED → dev `d21a1e4a`** (squash, normal — no `--admin`). Registered `mermaid`/`mermaid_mcp` in `pyproject.toml` markers, killing the `--strict-markers` collection-flake that was red-listing ALL craft CI when the env plugin install flaked. Full suite: **1729 passed / 38 skipped / 1 xfailed**. Resolves Next Action A.
- **Doc-gap closure → dev `88544709`** (workflow-engine documentation gap analysis): count drift fixed to 110/39 across README (6 refs), `docs/index.md`, cookbook, + categorical subtotals bump-version.sh misses (`hub.md` ORCHESTRATE 4→5 + Skills 38→39; `skills-agents.md` Orchestration 4→5); added the missing `/craft:orchestrate:workflow` hub row; added 2 mermaid flowcharts (mode-selection in help, execution-pipeline in command ref). Debunked a false agent finding (no `slug:` frontmatter exists in craft).

✅ Session 2026-06-13 (workflow-engine SHIPPED + PR-review fixes + CI infra bug surfaced):
- **PR #148 MERGED → dev `caadd8db`** (squash, `--admin`). Deterministic workflow engine (`/craft:orchestrate:workflow`, 3rd mode). Before merge, applied 2 review findings in the worktree (`d1d05880`): **B1** — `_resolve_binding` now fails closed as `WorkflowError` (not bare `KeyError`/`TypeError`) on missing/non-dict fields in both `[]`-flatten and `.field` paths (+2 regression tests, engine suite 55→57); **B2** — count drift (`package.json` 109→110/38→39; `plugin.json` breakdown `96+13`→`96+14`). Counts now 110 commands / 39 skills / 8 agents.
- **`--admin` bypass justified:** CI red on a pre-existing, infra-only failure — `pytest --strict-markers` errors collecting `test_mermaid_dogfood.py`/`test_mermaid_e2e.py` because `mermaid`/`mermaid_mcp` markers aren't in `pyproject.toml` (normally registered by an env pytest plugin whose install is currently flaking). **Identical on `dev`** (verified: dev's own CI run `27479702661` for `caadd8db` failed the same way) — unrelated to the PR diff. See Next Action A.

✅ Session 2026-06-13 (deprecation-drift fix SHIPPED + sibling bug found/fixed):
- **PR #147 MERGED → dev `1215e802`** (squash). Fixed the CI-invisible deprecation drift (`command-audit.sh --fix` stripping `deprecated`/`replaced-by`) AND a SECOND test-mutates-source bug found during verification: `test_docs_staleness.py` ran `docs-staleness-check.sh --fix` against the real tree, rewriting `108→109` count refs in 3 docs. Both fixed via isolated-copy helpers + structural guard `test_no_fix_invocation_targets_real_tree`; also fixed `test_on_expected_branch_type` to accept `fix/*`. Full `pytest tests/`: **1672 passed / 0 failed**, tree clean.
- `stash@{0}` (the combined 56-file + 3-doc drift fingerprint) dropped; fix worktree + branch removed. Bug class eliminated on dev. Closes Next-Action item A.

✅ Session 2026-06-13 (workflow-engine scaffold + deprecation-drift root cause + spec-docs mandate):
- **Workflow-engine spec** committed (`107ec13f`) + **FR9** (docs/tutorial/refcard/help-hub made ship-blocking). Worktree `feature/workflow-engine` scaffolded with ORCHESTRATE plan (5 increments); rebased onto dev `9ba88a6c` → at `e42afb06`. Implementation pending (new session).
- **Spec-docs mandate** (`d6de5070`): `brainstorm` Step 5.5 + `orchestrate:plan` template now REQUIRE a "Documentation & Discoverability" section in every captured spec/plan.
- **Deprecation-drift ROOT CAUSE CONFIRMED** via 15-agent workflow + manual code verify: `command-audit.sh --fix` (run by `test_audit_fix_mode_dry` against real tree) strips keys not in `VALID_FIELDS`. Fix spec written (`SPEC-command-audit-deprecated-fields-2026-06-13.md`); 2-file fix pending in `fix/command-audit-deprecated-fields` worktree. Memory `ci-invisible-deprecation-drift` upgraded to confirmed cause.
- `stash@{0}` (the 56-file drift) still parked — drop it once the fix lands.

✅ Session 2026-06-10 (homebrew generator deprecation fix — on dev, UNRELEASED):
- `commands/dist/homebrew.md` cask generator emitted deprecated `depends_on macos: ">= :{min_macos}"`; switched to bare-symbol form `depends_on macos: :{min_macos}` (min_macos is always a macOS codename, so the symbol is valid). Also fixed the reference SPEC-desktop-release-pipeline. Commit 6087ede1 on dev.
- Root cause for the warning that surfaced in Data-Wise/homebrew-tap (fixed there in PR #112/#113). Promote/ship on next craft release so regenerated casks stop reintroducing the warning.

✅ Session 2026-06-03 (three features shipped to dev):

PRs merged (all to dev):
- #143 (orchestrate:drive) — thin command + `skills/orchestration/drive-engine/` skill, two-stage termination (real verify gate), stop-at-green/no-auto-PR. +463/−2, 16 files.
- #144 (--refine flag) — `skills/workflow/prompt-refiner/` skill + `--refine` on brainstorm/do/orchestrate/plan:feature/arch:plan; rebased onto dev (plugin.json/CLAUDE.md/CHANGELOG count+entry conflicts resolved → 109 cmds / 38 skills). Scope-guard test hardened to key on `- name: refine` declaration (was false-flagging the /refine sunset-note prose). +396/−2, 24 files.
- #145 (version-sync set -e false-fail) — `|| true` on grep|head pipelines + `return 0` on trailing conditional; added `tests/test_version_sync.sh` regression (red/green proven). +12/−8 + test.

Post-merge dev state: 109 commands · 38 skills · 8 agents; validate-counts ✓; unit suite 23 passed. CHANGELOG `[Unreleased]` carries both feature entries (promote at next release).

Key learning: two parallel feature branches both bumping `plugin.json`/`CLAUDE.md` counts → the second to merge MUST rebase; both sides naively wrote "+1" so the resolved count is base+2 (37→38, not 37). Confirmed with validate-counts before continuing the rebase.

✅ Session 2026-05-15 (post-merge cleanup of v2.34.0 migration):

PRs shipped:
- #140 (35d170f4) — Post-merge drift sweep: 6 stale "37 skills" doc strings + `docs-staleness-check.sh` SKILL_COUNT over-counter (9th instance)
- #141 (52a58e49) — Skill coverage: documented 11 new skills in `docs/skills-agents.md` (3 new sections: Check, Dev, Workflow), removed spurious `release-checklist` reference-doc row that was inflating count by 1, dropped completed `docs/migration-plan.md`, fixed live SPEC link, swept 2 more over-counter instances (`post-release-sweep.sh` + workflow YAML in `commands/dist/homebrew.md` — 10th + 11th), added `bump-version.sh` block for `docs/skills-agents.md` `(N total)` header drift class
- Direct to dev (4c7a44bd) — Site refresh: README/index.md/REFCARD taglines updated from stale v2.33.0 carry-over to v2.34.0 in-development; REFCARD skill count 26→36, removed false 2026-05-11 release date

Knowledge captured:
- 6 new MEMORY.md entries: `(N total)` header drift class, spurious reference-docs-as-skills, review-loop PR pattern, squash-merge ahead/behind footgun, worktree cleanup from inside worktree, .STATUS Active Worktrees silent drift

Workflow observation: `/review` of merged PR uncovered next 1-3 follow-up bugs each cycle — this session's three commits (PR #140 → #141 → site update) all originated from review/sync feedback on the prior step.

Issues remaining open:
- #108 (Tauri/cask workflow revamp)
- #130 (himalaya-mcp + nexus-cli App secrets)
- #133 (hard_deny criterion #5 e2e gap)
- CHANGELOG curation gap: v2.34.0 content still under [Unreleased] — release pipeline will promote it when /release runs

✅ Sibling-plugin migration PRs (all MERGED 2026-05-10):

| Repo | PR | Merge commit | Notes |
|------|-----|--------------|-------|
| Data-Wise/aiterm | #10 | `09fa8f55` | Merged with `--admin` (pre-existing docs deploy failure unrelated) |
| Data-Wise/flow-cli | #443 | `111e8c8c` | CI green |
| Data-Wise/atlas | #15 | `8b15f643` | CI green |
| Data-Wise/mcp-bridge | #1 | `9b9cd233` | No required checks |

PAT → GitHub App auth migration ecosystem-complete: craft + 4 sibling plugins all on App auth. `HOMEBREW_TAP_GITHUB_TOKEN` secrets in those 4 repos can be deleted as cleanup. himalaya-mcp + nexus-cli (#130) still need App secrets configured first.
| Data-Wise/mcp-bridge | [#1](https://github.com/Data-Wise/mcp-bridge/pull/1) | Minimal swap (kept reusable workflow); no required checks |

✅ Session 2026-05-09 → 2026-05-10 (epic: 2 releases + ecosystem migration):

Releases shipped:
- v2.32.0 (2026-05-09) — GitHub-side branch protection + hook contract fix; 4 PRs bundled (#122 protect-baseline, #123 xref sweep, #124 polish, #125 hook fix)
- v2.32.1 (2026-05-10) — Infrastructure patch: durable Homebrew release auth (PAT → GitHub App migration; closes #129)

Major work:
- Audited GitHub branch protection across all Data-Wise repos: applied baseline (PR(0)+no force+no delete) to 47 repos (dev-tools + mediation + teaching forks); only homebrew-tap intentionally bare
- Built /craft:git:protect-baseline (script + slash command + reference docs + REFCARD + cookbook recipe + tutorial + cross-refs + CHANGELOG)
- 348-ref cross-ref sweep: 4 parallel agents, 49 files, naive sibling detection → 348 missing-refs reduced to 1 (legitimate skip)
- Hook contract fix (#125): pretooluse.py was reading CLAUDE_TOOL_NAME env vars Claude Code never sets — silently no-op'd in production. Fixed to read stdin JSON; migrated orphan e2e script + tutorial + Mermaid diagram. Discovered via rforge#1 code review.
- homebrew-release.yml PAT → GitHub App auth migration (#129). Validated end-to-end on real `release: published` event during v2.32.1 release.
- 4 sibling-plugin migration PRs in flight: aiterm #10, flow-cli #443, atlas #15, mcp-bridge #1 (deferred for review)

Issues filed:
- #126 docs: env-var follow-up to #125 (open)
- #127 badge_syncer false positives (open)
- #129 homebrew-release auth (CLOSED — fixed in v2.32.1)
- #130 himalaya-mcp + nexus-cli need App secrets configured (open)

Knowledge captured:
- MEMORY.md: 16 new entries across 2 batches (141 → 200 lines): macOS head portability, pre-commit Python interpreter, GitHub branch protection JSON quirks, CLI stderr-headers pattern, cross-branch parallel work pitfall, categorical subtotal drift, Claude Code hook contract, grep-all-callers lesson, behavioral vs source-presence tests, GitHub self-approve block, pre-commit auto-fix rollback, PAT expiration date-math, reusable workflows with App auth, same-App-across-repos, branch protection strict-mode false positive, release: published vs workflow_dispatch, asserting OLD pattern locks in deprecation, CHANGELOG [Unreleased] conversion gap, persistent post-release sweep files
- Updated global CLAUDE.md branch architecture (multi-branch craft-style vs single-integration patterns) + craft CLAUDE.md two-layer protection model
- Renamed v2.32.0 spec → v2.33.0 (Insights-Driven Improvements; today's v2.32.0 shipped a different scope)

Cleanup:
- Removed feature-fix-skill-structure + feature-fix-hook-stdin-contract + feature-protect-baseline + feature-protect-baseline-polish + feature-xref-sweep worktrees
- Pruned ~7 stale remote tracking refs across 2 sessions
- Deleted unused HOMEBREW_TAP_GITHUB_TOKEN secret from craft

✅ Session 2026-02-27 (v2.32.0 planning + spec approval):
- Reviewed BRAINSTORM-craft-insights-2026-02-27.md (5 features, P1-P4)
- Max-depth brainstorm: 2 agents, 8 expert questions, integration analysis
- Refined 5→4 features, deferred CI auto-diagnosis, deprioritized parallel agents
- Key decisions: extend /craft:check (not new command), soft warnings, CI SHA fix
- Captured + approved spec: SPEC-insights-improvements-v2.32-2026-02-27.md
- Resolved OQ1 (PR mode: warn only) + OQ2 (pre-flight: auto-run in Step 0)

✅ Session 2026-02-27 (v2.31.0 release):
- Completed v2.31.0 release pipeline, all downstream green
- Post-release sweep: 3 Tier 2 stale refs auto-fixed
- Fixed dogfood test for dynamic hub.md skill count template

---

🎯 Next Action:
A) ⚡ Start Phase 1 of `feature/multi-surface-release` in a NEW worktree session (Recommended) — `cd ~/.git-worktrees/craft/feature-multi-surface-release && claude`, then "Read `ORCHESTRATE-multi-surface-release.md` + the spec, start Phase 1 (release:verify-surfaces engine)". Phase 0 (rforge drift fix: `claude plugin update rforge`) is a standalone ops step, do anytime. NOTE: worktree HEAD has advanced past the planning commit (`1732ced0` ≠ `a73df283`) — a parallel session may already have started; check before assuming Phase 1 is untouched.
B) Resolve spec Open Q6 (aggregator host: reuse `Data-Wise/claude-plugins` vs new repo) — de-risks Phase 3; can do from this dev session (read-only inspect of the candidate repos).
C) Tackle #108 (Tauri/cask workflow revamp) — large feature, schedule for own release.
D) Take a break — branch-guard draft=dev shipped (#158/#159), spec approved, worktree ready.

Issues closed this session (2026-05-11):
- #126 (e30ba371) — env-var → stdin contract corrected in insights spec
- #127 (0e207c01) — badge_syncer false positives: branch_label + /badge/docs- classification
- #134 (c1cbb976) — branch-guard.sh path canonicalization production leak
- #135 (15a28409) — docs-staleness-check.sh BSD-sed bug (silent no-op on macOS)
- #136 (15a28409) — 19 stale skill paths in skills-agents.md

v2.33.0 Increment 1 (69778f59): /craft:check --version validator shipped

Issues remaining open (blocked on externals):
- #108 (Tauri/cask workflow revamp) — large feature scope, future release
- #130 (himalaya-mcp + nexus-cli App secrets) — requires user UI to set GitHub secrets
- #133 (hard_deny criterion #5 e2e gap) — blocked on Claude Code exposing classifier hook

## v2.33.0 Scope (spec: approved, untouched this session)

- Version sync validator (/craft:check --version) ~30 min
- Worktree pre-flight (4 checks, soft warnings) ~1.5h
- Homebrew SHA race fix (verify-tarball CI job) ~30 min
- Post-release sweep --fix (unified diff, mechanical auto-apply) ~30 min

Spec: docs/specs/SPEC-insights-improvements-v2.33-2026-02-27.md

## Future (v2.33.0+)

- CI Failure Auto-Diagnosis (pattern matching, deferred from v2.32.0)
- Headless Doc Sync (version refs only, prototype)
- himalaya-mcp release automation: version bump + changelog scripts
- Coaching mode (tool input modification — Phase 3 of guard design)
- Language detection for multi-lang projects

## Session Recap — 2026-06-22 — Planning Asset Audit
- Audited ~20 planning assets: craft / superpowers / user-level / official.
- Findings: C1 project-planner<->plan-orchestrator trigger collision (HIGH);
  C2 superpowers brainstorming HARD-GATE preempts craft pipeline (CRITICAL).
- Decisions D1-D12 locked (craft canonical, /plan dispatcher + tightened skills,
  upstream delegate/disable, drift-guard, all-4-lens, public-release bar).
- Spec: docs/specs/SPEC-planning-refactor-2026-06-22.md
- Next: execute A1-A8 (craft-only, ~60-90 min).

## Surfaces (v2.53.0)

| plugin | code | desktop | released | drift |
|--------|------|---------|----------|-------|
| craft | 2.53.0 | manual | 2.53.0 | no |
