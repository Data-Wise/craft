status: Active
version: 2.46.0
release_date: 2026-06-21
milestone: v2.46.0 SHIPPED 2026-06-21 — Governance Phase 2 PR #3 (PR #194→dev, #195→main, tag v2.46.0): soak-then-flip promotion + cross-repo wrapper, completing Phase 2 (visibility · prevention · promotion · cross-repo). soak.py = LOCAL gitignored STATE.json ledger (first_seen/last_seen/last_red per rule) fed by the SessionStart hook each audit; run_rules.py --promote-check (--window 14d, --state) lists warn rules soaked clean long enough to recommend a human warn→error flip (machinery recommends, human promotes; advisory exit 0). run.sh = cross-repo wrapper (consumers invoke the one installed engine, cwd-portable, no drift, R07). 8 dogfood tests (injected today → no flake), 47 governance suite green. Bundled #193: test_performance_cached_run → xfail(strict=False) (wall-clock microbenchmark flaked on shared CI, forced --admin; now a soft signal) — and #195 MERGED CLEAN with NO --admin (the xfail paid off). Homebrew tap synced to v2.46.0, main CI GREEN, post-release sweep ALL CLEAN. Counts: 113 / 39 / 8. R04 automation + #184 release-pre-flight guard remain deferred. Prior: v2.45.0 SHIPPED 2026-06-21 — Governance Phase 2 PR #2 (PR #190→dev, #192→main, tag v2.45.0): SessionStart visibility hook (governance/session_hook.py) — audits the live ~/.claude/skills tree at session open and injects a compact RED-only summary into context via SessionStart additionalContext; silent when clean, mtime-cached, no-op where skills tree absent, never breaks a session on error. Visibility not prevention (SessionStart hooks inject context, can't block). Completes the three-surface model: pre-commit + CI (prevention) · SessionStart (visibility). Implemented direct-to-worktree from the dev session (worktree-path-aware). #192 admin-merged (strict up-to-date BEHIND from the v2.44.0 merge-commit divergence; PR-context full suite GREEN). Homebrew tap synced to v2.45.0, main CI GREEN, post-release sweep ALL CLEAN. Counts: 113 / 39 / 8. Global install of the hook DEFERRED to post-v2.45.0 (point at released plugin path; docs in governance/README.md). PR #3 (soak-then-flip + cross-repo wrapper) + R04 deferred. Also: removed leaked ORCHESTRATE-guard-suite.md (PR #191). Prior: v2.44.0 SHIPPED 2026-06-21 — Governance Phase 2 PR #1 (PR #188→dev, #189→main, tag v2.44.0): R03 automated (manual→script — checks/no_private_in_public_marketplace.py scans a marketplace manifest, fails on a public-marketplace ref to a private/PII repo, denylist {savant}, dual-mode file/dir + good/bad fixtures; closes the last error-rule enforcement gap), pre-commit governance-gate hook (^governance/ → selftest + render --check drift) + CI gate in ci.yml (with explicit "CI does NOT evaluate R01/R07 live-env" note), engine {marketplace} substitution through audit()+selftest() + --marketplace. Built from a draft→adversarial-verify→ultra-finalize workflow chain (verified packet); implemented directly into the worktree from the dev session (branch-guard is worktree-path-aware — corrected a stale assumption). Also corrected stale "not yet wired" governance docs. #189 admin-merged (only failure was the flaky test_performance_cached_run timing test, unrelated to the diff; PR-context full suite GREEN). Homebrew tap synced to v2.44.0, main CI GREEN, post-release sweep ALL CLEAN. Counts: 113 commands / 39 skills / 8 agents. Phase 2 PR #2 (SessionStart hook + soak-then-flip + cross-repo wrapper) NEXT; R04 deferred. Prior: v2.43.0 SHIPPED 2026-06-20 — Skill-Ecosystem Governance (Phase 0) (PR #185→dev, #187→main, tag v2.43.0): policy-as-code under governance/ — RULES.yaml (8 skill-location rules, single source of truth), run_rules.py (audit + --selftest meta-validation, fail-closed), render_rules.py (CLAUDE.md block generation + --check drift gate), portable checkers + good/bad fixtures, guide + Plugin Release Runbook, 24 e2e/dogfood tests + governance pytest marker. PR #185 review hardening (all 5 issues): fail-closed gating (broken checker on error rule exits 1), recursive broken-symlink detection, R01 gates session (not ci) + visible canon-skip, --check exit-2, --selftest surfaces external rules, UTF-8. Homebrew tap synced to v2.43.0, main CI GREEN, post-release sweep ALL CLEAN. Counts: 113 commands / 39 skills / 8 agents. Phase 2 enforced-gates roadmap spec open in PR #186. Prior: v2.42.0 SHIPPED 2026-06-19 — /craft:code:fewer-prompts curated read-only Bash allowlist (PR #181). Prior: v2.41.1 SHIPPED 2026-06-19 — Documentation Coverage (PR #180): 25 tutorial stubs for all Phase 8 commands + broken link fix in TUTORIAL-smart-help.md. Prior: v2.41.0 SHIPPED 2026-06-19 — token-efficiency (quota gate, engine flag, parity runbook). v2.40.0 SHIPPED 2026-06-19 — Guard Suite (PR #174): /craft:git:guard + guards.json registry & ADHD toggle UX, no-switch-guard promoted to scripts/, guard-consistency validator, install-guards.sh. Destructive-restore reconciled to no-switch-guard; branch-guard rm-rf-.git/reset--hard softened to confirm (catastrophic ops stay NON-muteable). 111 commands. Review found 1 CRITICAL (catastrophic-bypass) + 6 issues — ALL fixed pre-merge; +5 registry tests; ci.yml now installs both guards + runs the no-switch suite. Prior: v2.39.0 SHIPPED 2026-06-19 — doc-coverage enforcement (PR #172→dev, #173→main). Prior: v2.38.2 SHIPPED 2026-06-15 — multi-surface release arc (3 patches). v2.38.0: multi-surface engine (verify-surfaces Step 13.6, cache-prune Step 13.7, Data-Wise aggregator marketplace) + branch-guard draft=dev (PRs #158/#159/#160/#161). Adversarial review found 3 HIGH + 6 MEDIUM/LOW bugs → v2.38.1 (PR #163: branch-aware dogfood tests fixed RED main CI; cache-prune data-loss; verify-surfaces git-tag no-op) → v2.38.2 (PR #165: corrupt-surface blocks, symlink GC, arg-parse, subshell). All 3 → main, main CI GREEN, tap + aggregator (Data-Wise/claude-plugins) pinned to 2.38.2, post-release sweep CLEAN. Counts: 112 commands / 39 skills / 8 agents. Prior: v2.37.0 SHIPPED 2026-06-13 — drift-prevention chain + CI triage/watch. Bundled brainstorm items 1-7 (PRs #151/#152/#153/#154/#155): #1 strict-markers guard + #4 workflow recipe discoverability; #7 bump-version categorical-subtotal sweep; #2 CI count-drift tripwire (README live counts); #5 /craft:ci:triage + #6 /craft:ci:watch. PR #156 → main 8c21a54d (merge commit), release tag v2.37.0. Downstream all GREEN: main CI verified, homebrew-release.yml updated tap to v2.37.0 (desc "112 commands", url v2.37.0.tar.gz). Post-release sweep ALL CLEAN (drift tooling paid off). Counts: 112 commands / 39 skills / 8 agents. Prior: v2.36.0 SHIPPED 2026-06-13 (PR #150 → main eb7e143d).
last_session: 2026-06-21

progress: v2.34.0 SHIPPED 2026-05-15 (PR #142 → main as 25c53c95, release tag v2.34.0). Bundled: commands→skills migration via PR #139 (3 batches, 11 new skills auto-activating from conversation context), PR #140 (post-merge drift sweep + 9th over-counter fix), PR #141 (skill coverage docs in skills-agents.md + bump-version sweep for `(N total)` headers + 10th + 11th over-counter instances), site refresh + .STATUS sync (4c7a44bd, 2b25c2a1), CHANGELOG promotion + tagline cleanup (cbb1a740). Test suite: 19 unit tests on test_craft_plugin.py (added test_skill_count_predicates_use_canonical_marker as a regression net for the recurring SKILL.md-vs-`*.md` over-counting bug — now fixed in 11 utilities total). Downstream all GREEN: docs.yml deployed, homebrew-release.yml updated tap to v2.34.0, brew install verified locally. Post-release sweep: ALL CLEAN, no Tier 2 drift.

📊 Branch Status:

| Branch | Head     | Base | Status                                                              |
|--------|----------|------|---------------------------------------------------------------------|
| main   | b1cfd87a | —    | Production (v2.46.0 tagged, PR #195 merged 2026-06-21, main CI GREEN) |
| dev    | ae71f327 | main | Released to main as v2.46.0 (clean, no --admin); tap synced |

📂 Active Worktrees:

- `feature/governance-184-release-guard` — R04 content-drift automation + #184 release advisory ([#196](https://github.com/Data-Wise/craft/pull/196)): R04 manual→script (`checks/no_drifted_copy.py` — installed SKILL.md must be byte-identical to canon; distinct from R07 version-pin; live env clean → soak-safe) + `pre-release-check.sh` advisory `run_rules.py --json` (never blocks, `TestReleaseGuard184`). 54 governance suite green. Worktree: `~/.git-worktrees/craft/governance-184-release-guard` (off dev b3c63e8a). Naïve "real-dir=copy" heuristic was dead (installs are real dirs, no symlinks) → redefined R04 as content-drift per advisor.

_Prior (all merged + worktrees removed 2026-06-21): `feature/governance-phase2-soak` (PR #194, soak-then-flip + cross-repo `run.sh` → dev); `feature/flaky-perf-xfail` (PR #193, xfail timing test); `feature/governance-phase2-hook` (PR #190, SessionStart hook → dev), `feature/cleanup-stray-orchestrate` (PR #191, removed leaked ORCHESTRATE), `feature/governance-phase2-gates` (PR #188, v2.44.0). Local branch refs left dangling (squash-merge + branch-guard blocks `-D`) — harmless._

> **🎯 Next Action (next session):**
> A) **`--swarm=2` spec:** `SPEC-orchestrate-swarm-2level-2026-06-18` — worktree pending. ⚡ (Recommended)
> B) **PAT→App migration:** himalaya-mcp + nexus-cli (#130).

_Prior: `feature/multi-surface-release` MERGED → dev `3814525d` (PR #161 squash, 2026-06-15, CI green) — all 5 phases (verify-surfaces engine, cache-prune, aggregator marketplace craft-side, docs; rforge drift fix ops). Full suite 1769 passed. ORCHESTRATE removed at merge (`a75dea82`); worktree removed; local branch `feature/multi-surface-release` left dangling (squash + unpushed rebase → `-d` refuses, `-D` guard-blocked — harmless). **Cross-repo aggregator DONE: `Data-Wise/claude-plugins#3` MERGED → main `45ab799d`** (2026-06-15) — `marketplace.json` listing craft/scholar/rforge/himalaya-mcp now live; `claude plugin marketplace add Data-Wise/claude-plugins` surfaces all. Q6 resolved to reuse claude-plugins. `feature/branch-guard-cleanup` MERGED → dev `772efcc7` (PR #160 squash, 2026-06-15, CI green) — #158/#159 review follow-up: consolidated dev/draft detection (single probe, `refs/heads/*`), test-header note + e2e re-indent; worktree + branches removed. `feature/ci-triage` MERGED → dev `a1812afb` (PR #154 squash, 2026-06-14, CI green) — item #5; first real test of #7+#2 (auto-swept CI subtotal, tripwire caught stale refs). `feature/pr2-drift-tripwire` MERGED → dev `f00f5872` (PR #153 squash, 2026-06-14, CI green) — completes the #7→#2 chain (subtotals synced + CI tripwire). `feature/pr7-bump-subtotals` MERGED → dev `91a82271` (PR #152 squash, 2026-06-13; CI caught a real BSD-sed-no-ops-on-GNU bug, fixed with a `sedi()` wrapper). `feature/pr1a-test-hygiene` MERGED → dev `813855f2` (PR #151 squash, 2026-06-13, CI green); worktree + local + remote branch all removed. `fix/mermaid-markers` MERGED → dev `d21a1e4a` (PR #149 squash); `feature/workflow-engine` MERGED → dev `caadd8db` (PR #148 squash) — worktrees + branches removed. `feature/orchestrate-drive`, `feature/refine-flag`, `fix/version-sync-pipefail` merged 2026-06-03 (PRs #143–#145)._

✅ Session 2026-06-19 (v2.42.0 release: fewer-prompts):
- **v2.42.0 SHIPPED** (PR #182 → main, `90f5b45d`). All CI green: Craft CI ✅, Docs Staleness ✅, Link Validation ✅, Markdown Linting ✅, Deploy Documentation ✅.
- **Release pipeline complete:** pre-flight → version bump (14 files) → semantic docs (CHANGELOG, index.md, mkdocs.yml) → PR #182 → merge conflict resolution (dev/main diverged: PR #181 squash-merged to main while dev had different history) → merge → GitHub release tag → docs auto-deployed via docs.yml → Homebrew tap auto-updated via homebrew-release.yml CI → post-release sweep ALL CLEAN.
- **Content shipped:** `/craft:code:fewer-prompts` — curated read-only Bash allowlist command (20 entries) with `--dry-run`, `--global`, `--reset` flags; idempotent via `craft_allowlist` tracking key. Stale-link CHANGELOG fix (test_no_broken_links was failing on markdown syntax in v2.41.1 entry).
- **Resolved:** GitHub issue #157 closed (branch-guard draft=dev — shipped PR #158, issue was stale).
- **Divergence note:** PR #181 (feature→main squash) ran before dev was synced, causing dev/main divergence at release time. Resolved by merging main into dev pre-release, then squash-merge proceeded cleanly.

✅ Session 2026-06-19 (v2.41.1 release: documentation coverage):
- **v2.41.1 SHIPPED** (PR #180 → main, `3e7ae7ab`). Merged with `--admin` (known "Validate Plugin Structure" phantom check). All substantive CI green: Link Validation ✅, Docs Staleness ✅, Markdown Linting ✅, Deploy Documentation ✅.
- **Release pipeline complete:** pre-flight (142 tests + 15 metadata checks) → version bump (14 files) → semantic docs (CHANGELOG, REFCARD, index.md, mkdocs.yml Tier 2 drift) → PR #180 → merge → GitHub release tag → docs auto-deployed via docs.yml → Homebrew tap auto-updated via homebrew-release.yml CI → post-release sweep ALL CLEAN.
- **Content shipped:** 25 tutorial stub files for all Phase 8 commands with mkdocs.yml nav entries; broken link fix in `TUTORIAL-smart-help.md` (TUTORIAL-do.md → smart-routing-tutorial.md, renamed v2.30.0). REFCARD.md Tier 2 drift fixed (counts were stale: 109 cmd/38 skills → 112/39).
- **Session context:** continuation from prior context-compaction session; CI was in_progress on PR #180 at handoff point.

✅ Session 2026-06-18 (orchestrate spec arc: session-lever reconciliation + 3 new specs):
- **Reconciled token-efficiency spec with session-level levers.** Revision note on `SPEC-orchestrate-token-efficiency-2026-06-17`: a global `CLAUDE_CODE_SUBAGENT_MODEL=sonnet` (highest priority) OVERRIDES orchestrator-v2's per-task haiku routing (Explore/Bash/test → haiku), forcing cheap reads up to sonnet (~5× regression) — so do NOT set it globally; craft self-routes. Main-session `Workflow`+`budget` documented as a flow-level practice (not craft — commands can't call it). Default-engine decision rule: derivable spec → `:workflow`, parallel writes → `--swarm`, else fan-out; worktrees = isolation, not a token lever.
- **3 new specs on `dev` (all pushed):** `SPEC-orchestrate-decomposition-2026-06-18` (design RESOLVED — mode-scaled agent soft cap, single-concern + ≤K-file sizing, reuse confirm gate, one-shot planning, fan-out path only); `SPEC-orchestrate-swarm-2level-2026-06-18` (READY — `--swarm` takes optional level, default 1, `--swarm=2` for two levels; bottom-up convergence + per-level test gate + non-overlap guard); `RESEARCH-hierarchical-worktrees-2026-06-18` (nested-dir worktrees = git anti-pattern; real shape = hierarchical branches over flat sibling worktrees, cap 2).
- **Commits:** `711e5a2c` (reconciliation) → `0a432dc1` (decomposition) → `6618c469` (research) → `88de8b4f` (2-level swarm). flow-cli `1907aadb`: dropped `CLAUDE_CODE_SUBAGENT_MODEL` export. Implementation of all three deferred to worktrees (Next Action E).

✅ Session 2026-06-17 (token-efficiency spec/plan + quota tooling + scheduled cloud run):
- **Investigation → APPROVED spec + 16-task plan.** From "is orchestrate the token-usage culprit?" → finding: cost is `agents × inherited-context × turns`; prompt caching is the only structural discount; worktrees cost ZERO tokens (corroborated by Anthropic's published multi-agent numbers). `docs/specs/SPEC-orchestrate-token-efficiency-2026-06-17.md` (APPROVED) + sibling `SPEC-context-floor-hygiene-2026-06-17.md` (skeleton) + `SPEC-workflow-as-default-engine-2026-06-17.md` (SUPERSEDED). Plan `docs/plans/2026-06-17-orchestrate-token-efficiency.md` (16 TDD tasks). All commits pushed to `dev`.
- **Adversarial review earned its keep twice:** killed an infeasible "platform `Workflow()` engine" premise (craft dispatches via Task; no host Workflow tool callable from a command) → re-scoped to craft's real deterministic `:workflow` mode as default (parity-gated); and caught a stale-quota trap before it shipped into Phase Q.
- **Methodology hardened (user pref):** parity gate is estimation-based (effect sizes + 95% CI + surprisal, NO NHST/p-thresholds); cost-weighted metric (output 5×, cache_read 0.1×, cache_creation 1.25×); `.craft/` run-marker reuse (not `.flow/`).
- **Phase Q appended:** `/craft:quota` pre-flight gate (historical median+interval estimate vs LIVE rate_limits) advising SAFE/TIGHT/DEFER; flow-cli `dash` consumer is an out-of-scope producer/consumer follow-on.
- **Quota/limits question resolved:** the native statusline `rate_limits` field (5h/weekly % + resets, Pro/Max after first API response) is THE programmatic source — no public quota CLI/API exists (user's own archived feature-request confirms); claude-hud's Keychain→oauth/usage cache was stale (frozen March). Memory added: `claude-code-native-statusline-rate-limits`, `craft-no-platform-workflow-tool`, `stats-estimation-not-nhst`.
- **Scheduled a cloud routine** (`trig_017orFX3wJv4QborUvFGtvHS`, Mon Jun 22 09:00 MDT, sonnet-4-6) to implement the pure-code tasks (parser + estimator + persister) TDD-style and open a PR to `dev` — deliberately timed for AFTER the weekly quota reset (user on Max 20x; pacing heavy work around the window).

✅ Session 2026-06-15 (branch-guard draft=dev shipped + multi-surface-release spec/worktree):
- **PR #158 MERGED → dev** — `branch-guard.sh` now treats research `draft` as `dev` (smart protection: new code blocked, existing + `.md` + commits allowed, force/reset blocked). Bundled 5 bash regression cases (Group 2b) + 1 pytest auto-detect case (`test_repo_with_draft_protects_both`). Backs the `~/.claude/rules/draft-as-dev-research.md` rule directly — no more manual self-enforcement.
- **PR #159 MERGED → dev** — wired the branch_guard bash suites into `ci.yml`: new "Install branch-guard hook" step + "Run branch-guard bash suites" (`tests/test_branch_guard.sh` + `_e2e.sh`) with `SKIP_PERF=1` gating the 50-invocation timing test. branch_guard behavior is now CI-validated, not local-only.
- **Brainstorm + SPEC (approved): multi-surface release.** Read-only investigation of craft's plugin dev→release pipeline → `docs/specs/SPEC-multi-surface-release-2026-06-15.md` (`9df211ed`). Key finding: Claude **Code** and **Desktop/Cowork** use SEPARATE plugin registries; Desktop CAN subscribe to a custom GitHub marketplace by `owner/repo` (`addMarketplace`, same engine as Code) — so "one source → all surfaces" is achievable via the `marketplace.json` craft already ships + a one-time `claude plugin marketplace add`. Item 1 (Desktop spike) DONE; D1–D7 decisions + 6 work items.
- **Worktree scaffolded:** `feature/multi-surface-release` + `ORCHESTRATE-multi-surface-release.md` (5 phases: rforge drift fix, verify-surfaces engine, cache-prune, aggregator marketplace, docs). Impl pending a NEW worktree session (ORCHESTRATE-STOP rule).
- **Memory:** new `claude-desktop-vs-code-plugin-stores.md` (separate stores + addMarketplace) so the investigation isn't redone. Global rule `draft-as-dev-research.md` updated (hook now enforces draft=dev, PR #158).

✅ Session 2026-06-13 (mermaid-marker CI fix + workflow-engine doc-gap closure):
- **PR #149 MERGED → dev `d21a1e4a`** (squash, normal — no `--admin`). Registered `mermaid`/`mermaid_mcp` in `pyproject.toml` markers, killing the `--strict-markers` collection-flake that was red-listing ALL craft CI when the env plugin install flaked. Full suite: **1729 passed / 38 skipped / 1 xfailed**. Resolves Next Action A.
- **Doc-gap closure → dev `88544709`** (workflow-engine documentation gap analysis): count drift fixed to 110/39 across README (6 refs), `docs/index.md`, cookbook, + categorical subtotals bump-version.sh misses (`hub.md` ORCHESTRATE 4→5 + Skills 38→39; `skills-agents.md` Orchestration 4→5); added the missing `/craft:orchestrate:workflow` hub row; added 2 mermaid flowcharts (mode-selection in help, execution-pipeline in command ref). Debunked a false agent finding (no `slug:` frontmatter exists in craft).

✅ Session 2026-06-13 (workflow-engine SHIPPED + PR-review fixes + CI infra bug surfaced):
- **PR #148 MERGED → dev `caadd8db`** (squash, `--admin`). Deterministic workflow engine (`/craft:orchestrate:workflow`, 3rd mode). Before merge, applied 2 review findings in the worktree (`d1d05880`): **B1** — `_resolve_binding` now fails closed as `WorkflowError` (not bare `KeyError`/`TypeError`) on missing/non-dict fields in both `[]`-flatten and `.field` paths (+2 regression tests, engine suite 55→57); **B2** — count drift (`package.json` 109→110/38→39; `plugin.json` breakdown `96+13`→`96+14`). Counts now 110 commands / 39 skills / 8 agents.
- **`--admin` bypass justified:** CI red on a pre-existing, infra-only failure — `pytest --strict-markers` errors collecting `test_mermaid_dogfood.py`/`test_mermaid_e2e.py` because `mermaid`/`mermaid_mcp` markers aren't in `pyproject.toml` (normally registered by an env pytest plugin whose install is currently flaking). **Identical on `dev`** (verified: dev's own CI run `27479702661` for `caadd8db` failed the same way) — unrelated to the PR diff. See Next Action A.

✅ Session 2026-06-13 (deprecation-drift fix SHIPPED + sibling bug found/fixed):
- **PR #147 MERGED → dev `1215e802`** (squash). Fixed the CI-invisible deprecation drift (`command-audit.sh --fix` stripping `deprecated`/`replaced-by`) AND a SECOND test-mutates-source bug found during verification: `test_docs_staleness.py` ran `docs-staleness-check.sh --fix` against the real tree, rewriting `108→109` count refs in 3 docs. Both fixed via isolated-copy helpers + structural guard `test_no_fix_invocation_targets_real_tree`; also fixed `test_on_expected_branch_type` to accept `fix/*`. Full `pytest tests/`: **1672 passed / 0 failed**, tree clean.
- `stash@{0}` (the combined 56-file + 3-doc drift fingerprint) dropped; fix worktree + branch removed. Bug class eliminated on dev. Closes Next-Action item A.

✅ Session 2026-06-13 (workflow-engine scaffold + deprecation-drift root cause + spec-docs mandate):
- **Workflow-engine spec** committed (`107ec13f`) + **FR9** (docs/tutorial/refcard/help-hub made ship-blocking). Worktree `feature/workflow-engine` scaffolded with ORCHESTRATE plan (5 increments); rebased onto dev `9ba88a6c` → at `e42afb06`. Implementation pending (new session).
- **Spec-docs mandate** (`d6de5070`): `brainstorm` Step 5.5 + `orchestrate:plan` template now REQUIRE a "Documentation & Discoverability" section in every captured spec/plan.
- **Deprecation-drift ROOT CAUSE CONFIRMED** via 15-agent workflow + manual code verify: `command-audit.sh --fix` (run by `test_audit_fix_mode_dry` against real tree) strips keys not in `VALID_FIELDS`. Fix spec written (`SPEC-command-audit-deprecated-fields-2026-06-13.md`); 2-file fix pending in `fix/command-audit-deprecated-fields` worktree. Memory `ci-invisible-deprecation-drift` upgraded to confirmed cause.
- `stash@{0}` (the 56-file drift) still parked — drop it once the fix lands.

✅ Session 2026-06-10 (homebrew generator deprecation fix — on dev, UNRELEASED):
- `commands/dist/homebrew.md` cask generator emitted deprecated `depends_on macos: ">= :{min_macos}"`; switched to bare-symbol form `depends_on macos: :{min_macos}` (min_macos is always a macOS codename, so the symbol is valid). Also fixed the reference SPEC-desktop-release-pipeline. Commit 6087ede1 on dev.
- Root cause for the warning that surfaced in Data-Wise/homebrew-tap (fixed there in PR #112/#113). Promote/ship on next craft release so regenerated casks stop reintroducing the warning.

✅ Session 2026-06-03 (three features shipped to dev):

PRs merged (all to dev):
- #143 (orchestrate:drive) — thin command + `skills/orchestration/drive-engine/` skill, two-stage termination (real verify gate), stop-at-green/no-auto-PR. +463/−2, 16 files.
- #144 (--refine flag) — `skills/workflow/prompt-refiner/` skill + `--refine` on brainstorm/do/orchestrate/plan:feature/arch:plan; rebased onto dev (plugin.json/CLAUDE.md/CHANGELOG count+entry conflicts resolved → 109 cmds / 38 skills). Scope-guard test hardened to key on `- name: refine` declaration (was false-flagging the /refine sunset-note prose). +396/−2, 24 files.
- #145 (version-sync set -e false-fail) — `|| true` on grep|head pipelines + `return 0` on trailing conditional; added `tests/test_version_sync.sh` regression (red/green proven). +12/−8 + test.

Post-merge dev state: 109 commands · 38 skills · 8 agents; validate-counts ✓; unit suite 23 passed. CHANGELOG `[Unreleased]` carries both feature entries (promote at next release).

Key learning: two parallel feature branches both bumping `plugin.json`/`CLAUDE.md` counts → the second to merge MUST rebase; both sides naively wrote "+1" so the resolved count is base+2 (37→38, not 37). Confirmed with validate-counts before continuing the rebase.

✅ Session 2026-05-15 (post-merge cleanup of v2.34.0 migration):

PRs shipped:
- #140 (35d170f4) — Post-merge drift sweep: 6 stale "37 skills" doc strings + `docs-staleness-check.sh` SKILL_COUNT over-counter (9th instance)
- #141 (52a58e49) — Skill coverage: documented 11 new skills in `docs/skills-agents.md` (3 new sections: Check, Dev, Workflow), removed spurious `release-checklist` reference-doc row that was inflating count by 1, dropped completed `docs/migration-plan.md`, fixed live SPEC link, swept 2 more over-counter instances (`post-release-sweep.sh` + workflow YAML in `commands/dist/homebrew.md` — 10th + 11th), added `bump-version.sh` block for `docs/skills-agents.md` `(N total)` header drift class
- Direct to dev (4c7a44bd) — Site refresh: README/index.md/REFCARD taglines updated from stale v2.33.0 carry-over to v2.34.0 in-development; REFCARD skill count 26→36, removed false 2026-05-11 release date

Knowledge captured:
- 6 new MEMORY.md entries: `(N total)` header drift class, spurious reference-docs-as-skills, review-loop PR pattern, squash-merge ahead/behind footgun, worktree cleanup from inside worktree, .STATUS Active Worktrees silent drift

Workflow observation: `/review` of merged PR uncovered next 1-3 follow-up bugs each cycle — this session's three commits (PR #140 → #141 → site update) all originated from review/sync feedback on the prior step.

Issues remaining open:
- #108 (Tauri/cask workflow revamp)
- #130 (himalaya-mcp + nexus-cli App secrets)
- #133 (hard_deny criterion #5 e2e gap)
- CHANGELOG curation gap: v2.34.0 content still under [Unreleased] — release pipeline will promote it when /release runs

✅ Sibling-plugin migration PRs (all MERGED 2026-05-10):

| Repo | PR | Merge commit | Notes |
|------|-----|--------------|-------|
| Data-Wise/aiterm | #10 | `09fa8f55` | Merged with `--admin` (pre-existing docs deploy failure unrelated) |
| Data-Wise/flow-cli | #443 | `111e8c8c` | CI green |
| Data-Wise/atlas | #15 | `8b15f643` | CI green |
| Data-Wise/mcp-bridge | #1 | `9b9cd233` | No required checks |

PAT → GitHub App auth migration ecosystem-complete: craft + 4 sibling plugins all on App auth. `HOMEBREW_TAP_GITHUB_TOKEN` secrets in those 4 repos can be deleted as cleanup. himalaya-mcp + nexus-cli (#130) still need App secrets configured first.
| Data-Wise/mcp-bridge | [#1](https://github.com/Data-Wise/mcp-bridge/pull/1) | Minimal swap (kept reusable workflow); no required checks |

✅ Session 2026-05-09 → 2026-05-10 (epic: 2 releases + ecosystem migration):

Releases shipped:
- v2.32.0 (2026-05-09) — GitHub-side branch protection + hook contract fix; 4 PRs bundled (#122 protect-baseline, #123 xref sweep, #124 polish, #125 hook fix)
- v2.32.1 (2026-05-10) — Infrastructure patch: durable Homebrew release auth (PAT → GitHub App migration; closes #129)

Major work:
- Audited GitHub branch protection across all Data-Wise repos: applied baseline (PR(0)+no force+no delete) to 47 repos (dev-tools + mediation + teaching forks); only homebrew-tap intentionally bare
- Built /craft:git:protect-baseline (script + slash command + reference docs + REFCARD + cookbook recipe + tutorial + cross-refs + CHANGELOG)
- 348-ref cross-ref sweep: 4 parallel agents, 49 files, naive sibling detection → 348 missing-refs reduced to 1 (legitimate skip)
- Hook contract fix (#125): pretooluse.py was reading CLAUDE_TOOL_NAME env vars Claude Code never sets — silently no-op'd in production. Fixed to read stdin JSON; migrated orphan e2e script + tutorial + Mermaid diagram. Discovered via rforge#1 code review.
- homebrew-release.yml PAT → GitHub App auth migration (#129). Validated end-to-end on real `release: published` event during v2.32.1 release.
- 4 sibling-plugin migration PRs in flight: aiterm #10, flow-cli #443, atlas #15, mcp-bridge #1 (deferred for review)

Issues filed:
- #126 docs: env-var follow-up to #125 (open)
- #127 badge_syncer false positives (open)
- #129 homebrew-release auth (CLOSED — fixed in v2.32.1)
- #130 himalaya-mcp + nexus-cli need App secrets configured (open)

Knowledge captured:
- MEMORY.md: 16 new entries across 2 batches (141 → 200 lines): macOS head portability, pre-commit Python interpreter, GitHub branch protection JSON quirks, CLI stderr-headers pattern, cross-branch parallel work pitfall, categorical subtotal drift, Claude Code hook contract, grep-all-callers lesson, behavioral vs source-presence tests, GitHub self-approve block, pre-commit auto-fix rollback, PAT expiration date-math, reusable workflows with App auth, same-App-across-repos, branch protection strict-mode false positive, release: published vs workflow_dispatch, asserting OLD pattern locks in deprecation, CHANGELOG [Unreleased] conversion gap, persistent post-release sweep files
- Updated global CLAUDE.md branch architecture (multi-branch craft-style vs single-integration patterns) + craft CLAUDE.md two-layer protection model
- Renamed v2.32.0 spec → v2.33.0 (Insights-Driven Improvements; today's v2.32.0 shipped a different scope)

Cleanup:
- Removed feature-fix-skill-structure + feature-fix-hook-stdin-contract + feature-protect-baseline + feature-protect-baseline-polish + feature-xref-sweep worktrees
- Pruned ~7 stale remote tracking refs across 2 sessions
- Deleted unused HOMEBREW_TAP_GITHUB_TOKEN secret from craft

✅ Session 2026-02-27 (v2.32.0 planning + spec approval):
- Reviewed BRAINSTORM-craft-insights-2026-02-27.md (5 features, P1-P4)
- Max-depth brainstorm: 2 agents, 8 expert questions, integration analysis
- Refined 5→4 features, deferred CI auto-diagnosis, deprioritized parallel agents
- Key decisions: extend /craft:check (not new command), soft warnings, CI SHA fix
- Captured + approved spec: SPEC-insights-improvements-v2.32-2026-02-27.md
- Resolved OQ1 (PR mode: warn only) + OQ2 (pre-flight: auto-run in Step 0)

✅ Session 2026-02-27 (v2.31.0 release):
- Completed v2.31.0 release pipeline, all downstream green
- Post-release sweep: 3 Tier 2 stale refs auto-fixed
- Fixed dogfood test for dynamic hub.md skill count template

---

🎯 Next Action:
A) ⚡ Start Phase 1 of `feature/multi-surface-release` in a NEW worktree session (Recommended) — `cd ~/.git-worktrees/craft/feature-multi-surface-release && claude`, then "Read `ORCHESTRATE-multi-surface-release.md` + the spec, start Phase 1 (release:verify-surfaces engine)". Phase 0 (rforge drift fix: `claude plugin update rforge`) is a standalone ops step, do anytime. NOTE: worktree HEAD has advanced past the planning commit (`1732ced0` ≠ `a73df283`) — a parallel session may already have started; check before assuming Phase 1 is untouched.
B) Resolve spec Open Q6 (aggregator host: reuse `Data-Wise/claude-plugins` vs new repo) — de-risks Phase 3; can do from this dev session (read-only inspect of the candidate repos).
C) Tackle #108 (Tauri/cask workflow revamp) — large feature, schedule for own release.
D) Take a break — branch-guard draft=dev shipped (#158/#159), spec approved, worktree ready.

Issues closed this session (2026-05-11):
- #126 (e30ba371) — env-var → stdin contract corrected in insights spec
- #127 (0e207c01) — badge_syncer false positives: branch_label + /badge/docs- classification
- #134 (c1cbb976) — branch-guard.sh path canonicalization production leak
- #135 (15a28409) — docs-staleness-check.sh BSD-sed bug (silent no-op on macOS)
- #136 (15a28409) — 19 stale skill paths in skills-agents.md

v2.33.0 Increment 1 (69778f59): /craft:check --version validator shipped

Issues remaining open (blocked on externals):
- #108 (Tauri/cask workflow revamp) — large feature scope, future release
- #130 (himalaya-mcp + nexus-cli App secrets) — requires user UI to set GitHub secrets
- #133 (hard_deny criterion #5 e2e gap) — blocked on Claude Code exposing classifier hook

## v2.33.0 Scope (spec: approved, untouched this session)

- Version sync validator (/craft:check --version) ~30 min
- Worktree pre-flight (4 checks, soft warnings) ~1.5h
- Homebrew SHA race fix (verify-tarball CI job) ~30 min
- Post-release sweep --fix (unified diff, mechanical auto-apply) ~30 min

Spec: docs/specs/SPEC-insights-improvements-v2.33-2026-02-27.md

## Future (v2.33.0+)

- CI Failure Auto-Diagnosis (pattern matching, deferred from v2.32.0)
- Headless Doc Sync (version refs only, prototype)
- himalaya-mcp release automation: version bump + changelog scripts
- Coaching mode (tool input modification — Phase 3 of guard design)
- Language detection for multi-lang projects
