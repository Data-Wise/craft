---
description: Git Safety Rails Guide
category: git
deprecated: true
replaced-by: "skills/dev/git/"
---

# Git Safety Rails Guide

> **This command is a thin shim.** The canonical reference content lives at
> [`skills/dev/git/references/safety-rails.md`](../../../skills/dev/git/references/safety-rails.md).
> This file exists only to preserve the `commands/git/docs/safety-rails.md`
> path (discoverable via `git:safety-rails`) through the v2.34.0 → v3.0.0
> migration.

## When invoked

Read
[`skills/dev/git/references/safety-rails.md`](../../../skills/dev/git/references/safety-rails.md)
and present it (or the relevant section) to the user. Do not duplicate the
content here — any update to the safety-rails guide belongs in the
reference file, which the `git-workflow` skill (`skills/dev/git/SKILL.md`,
Operation 11) surfaces on "is this safe?" / "what are the safety rails?"
triggers.

## Why this is a shim

`commands/git/docs/safety-rails.md` is one of the 4 `commands/git/docs/*.md`
reference docs (plus 10 `commands/git/*.md` commands) consolidated into
`skills/dev/git/` under the v2.34.0 → v3.0.0 migration (ADR-002's pattern,
generalized). At v3.0.0 this shim may be retired; the canonical reference
content in `skills/dev/git/references/` survives.
