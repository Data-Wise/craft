---
description: Git Commands Learning Guide
category: git
deprecated: true
replaced-by: "skills/dev/git/"
---

# Git Commands Learning Guide

> **This command is a thin shim.** The canonical reference content lives at
> [`skills/dev/git/references/learning-guide.md`](../../../skills/dev/git/references/learning-guide.md).
> This file exists only to preserve the `commands/git/docs/learning-guide.md`
> path (discoverable via `git:learning-guide`) through the v2.34.0 → v3.0.0
> migration.

## When invoked

Read
[`skills/dev/git/references/learning-guide.md`](../../../skills/dev/git/references/learning-guide.md)
and present it (or the relevant week's material) to the user. Do not
duplicate the content here — any update to the learning path belongs in the
reference file, which the `git-workflow` skill (`skills/dev/git/SKILL.md`,
Operation 11) surfaces on "teach me git" / "learning path" triggers.

## Why this is a shim

`commands/git/docs/learning-guide.md` is one of the 4 `commands/git/docs/*.md`
reference docs (plus 10 `commands/git/*.md` commands) consolidated into
`skills/dev/git/` under the v2.34.0 → v3.0.0 migration (ADR-002's pattern,
generalized). At v3.0.0 this shim may be retired; the canonical reference
content in `skills/dev/git/references/` survives.
