<!-- Planted defect (check 2, E1): the exact CLAUDE.md row that read GREEN for five minors. -->
<!-- The pre-existing scan matched only the plural "N agents", so "8 agent definitions" was invisible. -->

# Project Structure

| Directory | Purpose |
|-----------|---------|
| `commands/` | 48 commands (auto-discovered, one `.md` per command) |
| `agents/` | 8 agent definitions |
